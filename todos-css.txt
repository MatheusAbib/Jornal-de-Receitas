================================================================
ARQUIVO: components\Admin\AdminLayout\AdminLayout.css
================================================================
.admin-layout {
  min-height: 100vh;
  width: 100%;
}

.admin-content-wrapper {
  margin-left: 280px !important;
  margin-right: 0 !important;
  width: calc(100% - 280px) !important;
  max-width: none !important;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

@media (max-width: 768px) {
  .admin-content-wrapper {
    margin-left: 0 !important;
    margin-right: 0 !important;
    width: 100% !important;
    max-width: none !important;
  }
}

.admin-search-box {
  position: relative;
}

.admin-search-box input {
  padding: 12px 15px 12px 40px;
  border: 2px solid rgba(139, 0, 0, 0.2);
  font-size: 0.95rem;
  width: 280px;
  transition: all 0.25s ease;
  outline: none;
  background: #faf8f5;
  font-family: inherit;
}

.admin-search-box input:focus {
  border-color: var(--header-color);
  background: #fff;
  box-shadow: 0 0 0 3px rgba(139, 0, 0, 0.1);
}

.admin-search-box i {
  position: absolute;
  left: 14px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--accent-color);
  font-size: 0.9rem;
}

.admin-table-wrapper {
  flex: 1;
}

.admin-table-container {
  background: #fff;
  margin: 25px 35px;
  border: 2px solid var(--header-color);
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.15);
}

.admin-table-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 18px 25px;
  border-bottom: 2px solid var(--header-color);
}

.admin-table-header .count {
  font-family: 'DM Serif Display', serif;
  font-weight: 700;
  color: var(--header-color);
  font-size: 1.1rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.admin-table-header .count i {
  color: var(--accent-color);
}

.admin-table-scroll {
  overflow-x: auto;
}

.admin-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 0.9rem;
}

.admin-table thead {
  background: var(--header-color);
  color: #fff;
}

.admin-table th {
  padding: 14px 18px;
  text-align: left;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  font-size: 0.75rem;
}

.admin-table th.text-center {
  text-align: center;
}

.admin-table td {
  padding: 14px 18px;
  border-bottom: 1px dashed rgba(139, 0, 0, 0.2);
  vertical-align: middle;
  color: #333;
}

.admin-table tbody tr:last-child td {
  border-bottom: none;
}

.admin-table tbody tr:hover {
  background: #faf8f5;
}

.actions-group {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
}

.action-btn {
  width: 36px;
  height: 36px;
  border: none;
  background: none;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 0.95rem;
  transition: all 0.25s ease;
}

.action-btn:hover {
  transform: translate(-2px, -2px);
}

.admin-empty {
  text-align: center;
  padding: 60px 20px;
  border-top: 2px dashed rgba(139, 0, 0, 0.3);
}

.admin-empty i {
  font-size: 3.5rem;
  color: var(--header-color);
  opacity: 0.3;
  margin-bottom: 15px;
  display: block;
}

.admin-empty h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.4rem;
  color: var(--header-color);
  margin-bottom: 8px;
}

.admin-empty p {
  color: var(--light-text);
}

@media (max-width: 768px) {
  .admin-table-container {
    margin: 0px;
  }

  .admin-search-box input {
    width: 100%;
  }

  .admin-table thead {
    display: none;
  }

  .admin-table tr {
    display: block;
    margin-bottom: 15px;
    border: 2px solid var(--header-color);
    padding: 8px;
  }

  .admin-table td {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 8px 12px;
    border-bottom: 1px dashed rgba(139, 0, 0, 0.15);
  }

  .admin-table td::before {
    content: attr(data-label);
    font-weight: 700;
    color: var(--header-color);
    font-size: 0.75rem;
    text-transform: uppercase;
  }
}

.admin-topbar {
  background: #fff;
  padding: 20px 35px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 2px solid var(--header-color);
  flex-wrap: wrap;
  gap: 15px;
  box-shadow: 0 4px 0 rgba(139, 0, 0, 0.08);
}

.admin-topbar h1 {
  font-size: 1.6rem;
  color: var(--header-color);
  font-family: 'DM Serif Display', serif;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  display: flex;
  align-items: center;
  gap: 12px;
  margin: 0;
}


.admin-topbar-actions {
  display: flex;
  align-items: center;
  gap: 15px;
  flex-wrap: wrap;
}

.admin-topbar-actions span {
  color: var(--light-text);
  font-size: 0.88rem;
  font-weight: 600;
  display: inline-flex;
  align-items: center;
  gap: 8px;
}

.admin-topbar-actions i {
  color: var(--accent-color);
}

@media (max-width: 768px) {
  .admin-topbar {
    padding: 15px;
  }

  .admin-topbar h1 {
    font-size: 1.2rem;
  }
}

.admin-sidebar-overlay {
  display: none;
  position: fixed;
  inset: 0;
  z-index: 1500;
  background: rgba(0, 0, 0, 0.5);
  backdrop-filter: blur(4px);
  -webkit-backdrop-filter: blur(4px);
}

.admin-sidebar-overlay.open {
  display: block;
  animation: fadeInOverlay 0.3s ease;
}

@keyframes fadeInOverlay {
  from { opacity: 0; }
  to { opacity: 1; }
}

.admin-sidebar-toggle {
  display: none;
  position: absolute;
  top: 15px;
  left: 15px;
  width: 42px;
  height: 42px;
  background: white;
  border: var(--header-color);
  color: var(--header-color);
  border: 2px solid var(--header-color);
  font-size: 1.2rem;
  cursor: pointer;
  align-items: center;
  justify-content: center;
  transition: all 0.25s ease;
  z-index: 10;
}

.admin-sidebar-toggle:hover {
  background: #a00000;
  color: white;
}

@media (max-width: 768px) {
  .admin-sidebar-toggle {
    display: inline-flex;
  }

  .admin-sidebar {
    transform: translateX(-100%);
    transition: transform 0.3s ease;
    z-index: 2000;
  }

  .admin-sidebar.open {
    transform: translateX(0);
  }

  .admin-content-wrapper {
    margin-left: 0 !important;
    width: 100% !important;
    max-width: none !important;
  }

  .admin-topbar {
    padding-left: 70px !important;
  }
}

@media (max-width: 768px) {
  .admin-topbar {
    flex-direction: column;
    align-items: stretch;
    padding: 15px;
    padding-left: 70px;
    gap: 12px;
  }

  .admin-topbar h1 {
    font-size: 1.15rem;
  }

  .admin-topbar-actions {
    display: flex;
    flex-direction: column;
    align-items: stretch;
    gap: 10px;
    width: 100%;
  }

  .admin-topbar-actions > span {
    font-size: 0.8rem;
    justify-content: center;
    padding: 6px 10px;
    background: rgba(139, 0, 0, 0.04);
    border-left: 3px solid var(--accent-color);
  }

  .admin-search-box {
    width: 100%;
  }

  .admin-search-box input {
    width: 100% !important;
    font-size: 0.88rem;
    padding: 11px 14px 11px 40px;
  }
}

@media (max-width: 480px) {
  .admin-topbar {
    padding: 12px;
    padding-left: 65px;
  }

  .admin-topbar h1 {
    font-size: 1rem;
  }

  .admin-topbar-actions > span {
    font-size: 0.72rem;
  }

  .admin-search-box input {
    font-size: 0.82rem;
    padding: 10px 12px 10px 38px;
  }
}



================================================================
ARQUIVO: components\Admin\AdminSidebar\AdminSidebar.css
================================================================
.admin-sidebar {
  position: fixed;
  top: 0;
  left: 0;
  bottom: 0;
  width: 280px;
  background: #fff;
  border-right: 2px solid var(--header-color);
  display: flex;
  flex-direction: column;
  z-index: 1000;
  box-shadow: 4px 0 0 rgba(139, 0, 0, 0.15);
}

.admin-sidebar-header {
  background: var(--header-color);
  color: #fff;
  padding: 20px 22px;
}

.admin-sidebar-header h2 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.2rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.admin-user {
  font-size: 0.82rem;
  opacity: 0.9;
  font-weight: 600;
}

.admin-sidebar-nav {
  flex: 1;
  padding: 20px 15px;
  display: flex;
  flex-direction: column;
  gap: 8px;
  overflow-y: auto;
}

.admin-sidebar-nav a {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px 18px;
  color: var(--header-color);
  text-decoration: none;
  font-weight: 700;
  font-size: 0.82rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  background: transparent;
  border: 2px solid var(--header-color);
  transition: all 0.25s ease;
}

.admin-sidebar-nav a i {
  font-size: 1rem;
}

.admin-sidebar-nav a:hover {
  background: var(--header-color);
  color: #fff;
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.2);
}

.admin-sidebar-nav a i:hover {
  color: white;
}

.admin-sidebar-nav a.active {
  background: var(--accent-color);
  color: var(--header-color);
  border-color: var(--accent-color);
}

.admin-sidebar-footer {
  padding: 20px 15px;
  border-top: 2px solid var(--header-color);
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.admin-sidebar-btn {
  padding: 12px 18px;
  background: #fff;
  border: 2px solid var(--header-color);
  color: var(--header-color);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.78rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 10px;
  transition: all 0.25s ease;
}

.admin-sidebar-btn:hover {
  background: var(--header-color);
  color: #fff;
}

.admin-sidebar-btn-sair {
  color: #e74c3c;
  border-color: #e74c3c;
}

.admin-sidebar-btn-sair:hover {
  background: #e74c3c;
  color: #fff;
}

@media (max-width: 768px) {
  .admin-sidebar {
    transform: translateX(-100%);
    transition: transform 0.3s ease;
  }

  .admin-sidebar.open {
    transform: translateX(0);
  }
}

.admin-sidebar-close {
  display: none;
  position: absolute;
  top: 14px;
  right: 14px;
  background: none;
  border: none;
  color: #fff;
  font-size: 1.6rem;
  line-height: 1;
  cursor: pointer;
  opacity: 0.7;
  transition: all 0.25s ease;
}

.admin-sidebar-close:hover {
  opacity: 1;
  transform: rotate(90deg);
}

@media (max-width: 768px) {
  .admin-sidebar-close {
    display: block;
  }
}



================================================================
ARQUIVO: components\Global\Carrossel\Carrossel.css
================================================================
.carousel-container {
  width: 100%;
  max-width: 1200px;
  margin: 80px auto auto;
  height: 500px;
  border: 2px solid var(--header-color);
  box-shadow: 6px 6px 0 rgba(139, 0, 0, 0.15);
  position: relative;
  overflow: hidden;
}

.carousel {
  width: 100%;
  height: 100%;
  position: relative;
}

.carousel-inner {
  display: flex;
  height: 100%;
  transition: transform 0.6s ease;
}

.carousel-item {
  min-width: 100%;
  height: 100%;
  position: relative;
}

.carousel-item img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.carousel-caption {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 40px 30px;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.8), transparent);
  color: #fff;
  text-align: left;
}

.carousel-caption h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 2rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 8px;
  color: var(--accent-color);
}

.carousel-caption p {
  font-size: 1rem;
  margin: 0;
  opacity: 0.95;
}

.carousel-controls {
  position: absolute;
  top: 50%;
  left: 0;
  right: 0;
  display: flex;
  justify-content: space-between;
  padding: 0 20px;
  transform: translateY(-50%);
  pointer-events: none;
}

.carousel-control {
  width: 50px;
  height: 50px;
  background: rgba(255, 255, 255, 0.9);
  border: 2px solid var(--header-color);
  color: var(--header-color);
  font-size: 1.2rem;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.25s ease;
  pointer-events: auto;
  box-shadow: 3px 3px 0 rgba(0, 0, 0, 0.2);
}

.carousel-control:hover {
  background: var(--header-color);
  color: #fff;
  box-shadow: 5px 5px 0 rgba(0, 0, 0, 0.3);
}

.carousel-indicators {
  position: absolute;
  bottom: 15px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  gap: 8px;
  z-index: 5;
}

.carousel-indicator {
  width: 30px;
  height: 5px;
  background: rgba(255, 255, 255, 0.5);
  cursor: pointer;
  transition: all 0.25s ease;
  border: 1px solid rgba(0, 0, 0, 0.3);
}

.carousel-indicator.active {
  background: var(--accent-color);
  width: 45px;
}

.carousel-indicator:hover {
  background: rgba(255, 255, 255, 0.8);
}



================================================================
ARQUIVO: components\Global\Header\Header.css
================================================================
.header-full-width {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    width: 100%;
    background: linear-gradient(135deg, #8b0000, var(--header-bg));
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    transition: padding 0.4s ease, box-shadow 0.4s ease, background 0.4s ease;
    transform: translateY(0);
}

.header-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 15px 20px;
    transition: padding 0.4s ease;
}

.newspaper-header {
    position: relative;
    padding: 10px 20px;
    text-align: center;
}

.header-top {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: 15px;
}

.header-left {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    gap: 10px;
    justify-self: start;
}

.header-center {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 4px;
    justify-self: center;
}

.header-right {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 12px;
    justify-self: end;
}

.header-greeting-date {
    margin-top: 0;
    padding: 0;
    font-size: 0.9rem;
    opacity: 0.7;
    background: transparent;
}

.newspaper-title {
    position: relative;
    display: inline-block;
    margin: 0;
    padding: 0 20px;
    font-family: 'DM Serif Display', serif;
    font-size: 3.5rem;
    font-weight: 800;
    line-height: 1.1;
    color: white;
    text-transform: uppercase;
    letter-spacing: 3px;
    transition: all 0.4s ease;
}

.newspaper-title::before,
.newspaper-title::after {
    content: "✷";
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    color: var(--accent-color);
    font-size: 2.5rem;
    text-shadow: none;
    transition: all 0.4s ease;
}

.newspaper-title::before {
    left: -35px;
}

.newspaper-title::after {
    right: -35px;
}

.newspaper-subtitle {
    margin: 5px 0;
    font-size: 1rem;
    font-style: italic;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.9);
    letter-spacing: 1px;
    transition: all 0.4s ease;
}

.newspaper-date {
    font-size: 0.85rem;
    font-weight: 600;
    color: white;
    white-space: nowrap;
    background: transparent;
    transition: all 0.25s ease;
}

.newspaper-price {
    padding: 8px 18px;
    font-size: 0.7rem;
    font-weight: 800;
    color: var(--header-color);
    white-space: nowrap;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    background: var(--accent-color);
    border: 2px solid var(--accent-color);
    transition: all 0.25s ease;
    
}

.header-nav {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
    width: 100%;
    margin-top: 20px;
    padding-top: 18px;
    border-top: 1px solid rgba(255, 255, 255, 0.15);
    transition: all 0.4s ease;
    
}

.header-nav a {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 9px 18px;
    font-size: 0.7rem;
    font-weight: 700;
    color: white;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    background: transparent;
    border: 2px solid rgba(255, 255, 255, 0.25);
    transition: all 0.25s ease;
}

.header-nav a i {
    font-size: 0.85rem;
    opacity: 0.9;
}

.header-nav a:hover {
    color: var(--header-color);
    background: white;
    border-color: white;
    box-shadow: 3px 3px 0 rgba(255, 255, 255, 0.3);
}

.header-nav a.active {
    color: var(--header-color);
    font-weight: 800;
    background: var(--accent-color);
    border-color: var(--accent-color);
}

.header-nav a.active i {
    color: var(--header-color);
    opacity: 1;
}

.header-nav a.active:hover {
    color: var(--header-color);
    background: var(--accent-color);
    border-color: var(--accent-color);
    box-shadow: 3px 3px 0 rgba(212, 175, 55, 0.4);
}

.header-user-profile,
.header-logout-btn,
.header-notification-btn {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    width: fit-content;
    height: 42px;
    padding: 0 18px;
    box-sizing: border-box;
    color: white;
    font-size: 0.7rem;
    font-weight: 700;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    cursor: pointer;
    background: rgba(255, 255, 255, 0.06);
    border: 2px solid rgba(255, 255, 255, 0.25);
    transition: all 0.25s ease;
}

.header-user-profile:hover,
.header-logout-btn:hover,
.header-notification-btn:hover {
    color: var(--header-color);
    background: white;
    border-color: white;
    box-shadow: 3px 3px 0 rgba(255, 255, 255, 0.3);
}

.header-user-profile i,
.header-logout-btn i,
.header-notification-btn i {
    font-size: 0.9rem;
}


.header-user-mobile,
.header-logout-sidebar,
.header-notification-sidebar,
.menu-icon,
.header-nav-title,
.header-nav-close,
.header-nav-overlay {
    display: none;
}

.header-nav-overlay.open {
    position: fixed;
    top: 0;
    left: 0;
    z-index: 1500;
    display: block;
    width: 100vw;
    height: 100vh;
    background: rgba(0, 0, 0, 0.35);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    animation: fadeInOverlay 0.3s ease;
}

@keyframes fadeInOverlay {
    from {
        opacity: 0;
    }

    to {
        opacity: 1;
    }
}

.header-notification-desktop {
    display: none;
}

.header-notification-nav {
    display: inline-flex;
}

.header-notification-sidebar {
    display: none !important;
}

.notification-sidebar-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 19px;
    height: 19px;
    margin-left: auto;
    padding: 0 5px;
    box-sizing: border-box;
    color: var(--header-color);
    font-size: 0.65rem;
    font-weight: 800;
    line-height: 1;
    background: var(--accent-color);
    border-radius: 50%;
}

.notification-badge {
    position: absolute;
    top: -8px;
    right: -8px;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 19px;
    height: 19px;
    padding: 0 5px;
    box-sizing: border-box;
    color: var(--header-color);
    font-size: 0.65rem;
    font-weight: 800;
    line-height: 1;
    background: #d4af37;
    border: 2px solid var(--header-color);
    border-radius: 50%;
}

.nova-notificacao {
    z-index: 3000;
    color: #fff !important;
    background: #4f8fc4 !important;
    border-color: #2f6f9f !important;
}

.nova-notificacao i {
    color: #fff;
}

.header-full-width.scrolled {
    background: linear-gradient(135deg, #8b0000db, var(--header-bg));
    box-shadow: 0 2px 15px rgba(0, 0, 0, 0.15);
    transition: none;
}

.header-full-width.scrolled .header-container {
    padding: 0;
}

.header-full-width.scrolled .newspaper-subtitle {
    max-height: 0;
    margin: 0;
    padding: 0;
    overflow: hidden;
    opacity: 0;
}

.header-full-width.scrolled .header-top {
    margin-bottom: 0;
}

.header-full-width.scrolled .newspaper-title {
    padding: 0 30px;
    font-size: 2rem;
}

.header-full-width.scrolled .newspaper-title::before,
.header-full-width.scrolled .newspaper-title::after {
    display: none;
}


.header-full-width.scrolled .newspaper-price,
.header-full-width.scrolled .header-user-profile {
    font-size: 0.7rem;
}

.header-full-width.scrolled .newspaper-date {
    display: none;
}

.header-full-width.scrolled .header-notification-nav {
    display: none;
}

.header-full-width.scrolled .header-notification-desktop {
    display: inline-flex;
}

.header-full-width.scrolled .menu-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 38px;
    height: 38px;
    padding: 0;
    flex-shrink: 0;
    color: white;
    font-size: 1.1rem;
    cursor: pointer;
    background: rgba(255, 255, 255, 0.06);
    border: 2px solid rgba(255, 255, 255, 0.3);
    transition: all 0.25s ease;
}
.header-full-width.scrolled .header-center {
    gap: 0;
}
.header-full-width.scrolled .menu-icon:hover {
    color: var(--header-color);
    background: white;
    border-color: white;
}

.header-full-width.scrolled .header-nav:not(.open) {
    max-height: 0;
    margin: 0;
    padding: 0;
    overflow: hidden;
    opacity: 0;
    border: none;
    transform: translateX(-100%);
    transition: none;
}

.header-full-width.scrolled .header-nav.open {
    position: fixed;
    top: 0;
    left: 0;
    z-index: 2000;
    display: flex;
    flex-direction: column;
    align-items: stretch;
    justify-content: flex-start;
    gap: 8px;
    width: 350px;
    max-width: 85%;
    height: 100vh;
    max-height: none;
    margin: 0;
    padding: 22px;
    overflow-y: auto;
    opacity: 1;
    flex-wrap: nowrap;
    background: #ffffffed;
    border: none;
    border-right: 2px solid var(--header-color);
    box-shadow: 4px 0 0 rgba(139, 0, 0, 0.15);
    transform: translateX(0);
    transition: none;
}

.header-full-width.scrolled .header-nav.open .header-nav-top,
.header-nav.open .header-nav-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding-bottom: 14px;
    margin-bottom: 12px;
    border-bottom: 2px solid var(--header-color);
}

.header-full-width.scrolled .header-nav.open .header-nav-title,
.header-nav.open .header-nav-title {
    display: block;
    margin: 0;
    color: var(--header-color);
    font-family: 'DM Serif Display', serif;
    font-size: 1.2rem;
    font-weight: 800;
    line-height: 1.1;
    text-transform: uppercase;
    letter-spacing: 2px;
}

.header-full-width.scrolled .header-nav.open .header-nav-close,
.header-nav.open .header-nav-close {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    color: var(--header-color);
    font-size: 1.8rem;
    line-height: 1;
    cursor: pointer;
    background: none;
    border: none;
    opacity: 0.7;
    transition: all 0.3s ease;
}

.header-full-width.scrolled .header-nav.open .header-nav-close:hover,
.header-nav.open .header-nav-close:hover {
    opacity: 1;
    transform: rotate(90deg);
}

.header-full-width.scrolled .header-nav.open > a {
    justify-content: flex-start;
    padding: 14px 18px;
    color: var(--header-color);
    font-size: 0.85rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    background: transparent;
    border: 2px solid var(--header-color);
    transform: none;
}

.header-full-width.scrolled .header-nav.open > a i {
    color: var(--header-color);
    opacity: 1;
}

.header-full-width.scrolled .header-nav.open > a:hover {
    color: #fff;
    background: var(--header-color);
    border-color: var(--header-color);
    transform: none;
    box-shadow: none;
}

.header-full-width.scrolled .header-nav.open > a:hover i {
    color: #fff;
}

.header-full-width.scrolled .header-nav.open > a.active {
    color: var(--header-color);
    background: var(--accent-color);
    border-color: var(--accent-color);
}

.header-full-width.scrolled .header-nav.open > a.active i {
    color: var(--header-color);
}

.header-full-width.scrolled .header-nav.open .header-user-profile {
    width: 100%;
    min-height: 46px;
    height: auto;
    padding: 14px 18px;
    justify-content: flex-start;
    box-sizing: border-box;
    color: var(--header-color);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    background: transparent;
    border: 2px solid var(--header-color);
    transform: none;
}

.header-full-width.scrolled .header-nav.open .header-user-profile i,
.header-full-width.scrolled .header-nav.open .header-user-profile span {
    color: var(--header-color);
}

.header-full-width.scrolled .header-nav.open .header-user-profile:hover {
    color: #fff;
    background: var(--header-color);
    border-color: var(--header-color);
    transform: none;
    box-shadow: none;
}

.header-full-width.scrolled .header-nav.open .header-user-profile:hover i,
.header-full-width.scrolled .header-nav.open .header-user-profile:hover span {
    color: #fff;
}

.header-full-width.scrolled .header-nav.open .header-user-mobile,
.header-full-width.scrolled .header-nav.open .header-notification-sidebar,
.header-full-width.scrolled .header-nav.open .header-notification-nav,
.header-full-width.scrolled .header-nav.open .header-logout-sidebar {
    display: none !important;
}

.header-nav a.header-logout-sidebar {
    display: none;
}


.header-nav a i,
.header-user-profile i,
.header-logout-btn i,
.header-notification-btn i,
.menu-icon i {
  font-size: 1rem;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.newspaper-title-link {
  text-decoration: none;
  color: inherit;
  display: inline-block;
  cursor: pointer;
  transition: opacity 0.25s ease;
}

.newspaper-title-link:hover {
  opacity: 0.85;
}

.newspaper-title-link,
.newspaper-title-link .newspaper-title {
  cursor: pointer;
  pointer-events: auto;
  position: relative;
  z-index: 2;
}

.header-full-width.scrolled .newspaper-title-link {
  display: inline-block;
  padding: 0;
  margin: 0;
}

.header-full-width.scrolled .newspaper-title-link .newspaper-title {
  pointer-events: none;
}

.newspaper-title.clicavel {
  cursor: pointer;
  transition: opacity 0.25s ease;
}

.newspaper-title.clicavel:hover {
  opacity: 0.85;
}


.menu-icon i,
.header-nav-close i {
  pointer-events: none;
}

.menu-icon,
.header-nav-close {
  pointer-events: auto;
  cursor: pointer;
}


================================================================
ARQUIVO: components\Global\ImagemLoader\ImagemLoader.css
================================================================
.imagem-loader-wrapper {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
  background: #f5f0eb;
}

.imagem-loader-wrapper img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  opacity: 0;
  transition: opacity 0.4s ease;
}

.imagem-loader-wrapper img.loaded {
  opacity: 1;
}

.imagem-loader-spinner {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 40px;
  height: 40px;
  margin: -20px 0 0 -20px;
  border: 4px solid rgba(212, 175, 55, 0.2);
  border-top-color: #d4af37;
  border-radius: 50%;
  animation: imagem-loader-spin 0.8s linear infinite;
  z-index: 1;
}

@keyframes imagem-loader-spin {
  0%   { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.imagem-loader-fallback {
  position: relative;
  width: 100%;
  height: 100%;
  min-height: 200px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8px;
  background: #f5f0eb;
  color: rgba(0, 0, 0, 0.4);
  font-size: 0.85rem;
  text-align: center;
}

.imagem-loader-fallback i {
  font-size: 2rem;
}



================================================================
ARQUIVO: components\Global\LoaderInline\LoaderInline.css
================================================================
.loader-inline {
  text-align: center;
  padding: 60px 20px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 18px;
}

.loader-inline-spinner {
  position: relative;
  width: 55px;
  height: 55px;
}

.loader-inline-ring {
  position: absolute;
  inset: 0;
  border: 3px solid transparent;
  border-top-color: #8b0000;
  border-radius: 50%;
  animation: loader-inline-spin 1.1s cubic-bezier(0.5, 0, 0.5, 1) infinite;
}

.loader-inline-ring:nth-child(2) {
  inset: 7px;
  border-top-color: #d4af37;
  animation-duration: 1.4s;
  animation-direction: reverse;
}

.loader-inline-ring:nth-child(3) {
  inset: 14px;
  border-top-color: #8b0000;
  animation-duration: 1.8s;
}

@keyframes loader-inline-spin {
  0%   { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.loader-inline-text {
  font-family: 'DM Serif Display', Georgia, serif;
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: #8b0000;
  margin: 0;
}



================================================================
ARQUIVO: components\Global\PageLoader\PageLoader.css
================================================================
.page-loader {
  position: fixed;
  inset: 0;
  z-index: 99999;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #fdfaf3;
  opacity: 1;
  visibility: visible;
  transition: opacity 0.4s ease, visibility 0.4s ease;
}

.page-loader.is-hidden {
  opacity: 0;
  visibility: hidden;
  pointer-events: none;
}

.loader-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 22px;
}

.loader-spinner {
  position: relative;
  width: 70px;
  height: 70px;
}

.loader-ring {
  position: absolute;
  inset: 0;
  border: 3px solid transparent;
  border-top-color: #8b0000;
  border-radius: 50%;
  animation: loader-spin 1.1s cubic-bezier(0.5, 0, 0.5, 1) infinite;
}

.loader-ring:nth-child(2) {
  inset: 8px;
  border-top-color: #d4af37;
  animation-duration: 1.4s;
  animation-direction: reverse;
}

.loader-ring:nth-child(3) {
  inset: 16px;
  border-top-color: #8b0000;
  animation-duration: 1.8s;
}

@keyframes loader-spin {
  0%   { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.loader-text {
  margin: 0;
  font-family: 'DM Serif Display', Georgia, serif;
  font-size: 0.95rem;
  font-weight: 700;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: #8b0000;
}

.loader-dots span {
  display: inline-block;
  animation: loader-dot 1.4s infinite ease-in-out both;
}

.loader-dots span:nth-child(1) { animation-delay: 0s; }
.loader-dots span:nth-child(2) { animation-delay: 0.2s; }
.loader-dots span:nth-child(3) { animation-delay: 0.4s; }

@keyframes loader-dot {
  0%, 80%, 100% { opacity: 0.2; transform: translateY(0); }
  40%           { opacity: 1;   transform: translateY(-3px); }
}

@media (prefers-reduced-motion: reduce) {
  .loader-ring,
  .loader-dots span {
    animation: none;
  }
}



================================================================
ARQUIVO: components\Global\PageWrapper\PageWrapper.css
================================================================
.page-wrapper {
  flex: 1;
  display: flex;
  flex-direction: column;
}



================================================================
ARQUIVO: components\Global\Rodape\Rodape.css
================================================================
.newspaper-footer {
  background: transparent;
  border-top: 1px solid rgba(139, 0, 0, 0.3);
}

.footer-content {
  width: 1400px;
  margin: 0 auto;
  padding: 35px 0px 35px;
  display: grid;
  grid-template-columns: 2fr 1fr 1fr;
  gap: 40px;
}

.footer-section {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.footer-title {
  font-family: 'DM Serif Display', serif;
  font-size: 1.1rem;
  text-transform: uppercase;
  letter-spacing: 1px;
  color: var(--header-color);
  margin: 0 0 6px 0;
  padding-bottom: 10px;
  border-bottom: 1px solid rgba(139, 0, 0, 0.3);
}

.footer-description {
  font-size: 0.9rem;
  line-height: 1.7;
  color: var(--light-text);
  margin: 0;
}

.footer-links {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.footer-links li a {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  color: var(--light-text);
  text-decoration: none;
  font-size: 0.8rem;
  font-weight: 600;
  transition: all 0.25s ease;
  padding: 4px 0;
}

.footer-links li a i {
  font-size: 0.85rem;
  transition: all 0.25s ease;
}

.footer-links li a:hover {
  color: var(--header-color);
  transform: translateX(4px);
}

.footer-links li a:hover i {
  color: var(--header-color);
}

.social-links {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}

.social-links a {
  width: 38px;
  height: 38px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: var(--header-color);
  border: 2px solid var(--header-color);
  color: #fff;
  font-size: 0.95rem;
  text-decoration: none;
  transition: all 0.25s ease;
}

.social-links a:hover {
  background: var(--accent-color);
  color: white;
  border-color: var(--accent-color);
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.25);
}

.footer-bottom {
  border-top: 1px dashed rgba(139, 0, 0, 0.2);
  padding: 20px 30px;
  text-align: center;
}

.footer-bottom p {
  font-size: 0.65rem;
  color: var(--light-text);
  margin: 0;
  letter-spacing: 0.5px;
  text-transform: uppercase;
  font-weight: 600;
}

@media (max-width: 768px) {
  .footer-content {
    grid-template-columns: 1fr;
    gap: 30px;
    padding: 30px 8px 20px;
    width: auto;
  }

  .footer-title {
    font-size: 1rem;
  }

  .footer-description {
    font-size: 0.85rem;
  }

  .footer-bottom {
    padding: 16px 20px;
  }

  .footer-bottom p {
    font-size: 0.7rem;
  }
}

@media (max-width: 480px) {
  .newspaper-footer {
    margin-top: 40px;
  }


  .social-links a {
    width: 38px;
    height: 38px;
    font-size: 1rem;
  }
}



================================================================
ARQUIVO: components\Home\Button\Button.css
================================================================
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 11px 22px;
  border: none;
  font-family: inherit;
  font-weight: 700;
  font-size: 0.8rem;
  cursor: pointer;
  transition: all 0.25s ease;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  min-width: 100px;
}

.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  pointer-events: none;
}

.btn-primary {
  background: var(--header-color);
  color: #fff;
}

.btn-primary:hover {
  background: #a00000;
  transform: translate(-2px, -2px);
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.25);
}

.btn-success {
  background: #27ae60;
  color: #fff;
}

.btn-success:hover {
  background: #1e8449;
  box-shadow: 4px 4px 0 rgba(39, 174, 96, 0.4);
}

.btn-danger {
  background: #e74c3c;
  color: #fff;
}

.btn-danger:hover {
  background: #b91c1c;
  box-shadow: 4px 4px 0 rgba(231, 76, 60, 0.4);
}

.btn-cancel {
  background: #fff;
  color: var(--light-text);
}

.btn-cancel:hover {
  background: var(--header-color);
  color: #fff;
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.25);
}

.btn-loading {
  pointer-events: none;
  opacity: 0.7;
}

.btn-loading i {
  margin-right: 4px;
}



================================================================
ARQUIVO: components\Home\CardReceita\CardReceita.css
================================================================
.card {
  background: #fff;
  border: 2px solid var(--header-color);
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.15);
  display: flex;
  flex-direction: column;
  transition: all 0.25s ease;
}

.card:hover {
  box-shadow: 7px 7px 0 rgba(139, 0, 0, 0.2);
}

.card-image {
  position: relative;
  width: 100%;
  height: 200px;
  overflow: hidden;
  background: #f0ece6;
  border-bottom: 2px solid var(--header-color);
}

.card-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.card-badge-categoria {
  position: absolute;
  top: 0px;
  left: 0px;
  padding: 4px 10px;
  font-size: 0.7rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  display: inline-flex;
  align-items: center;
  gap: 5px;
  color: #fff;
  border: none;
}

.card-badge-categoria.salgado { background: var(--header-color); }
.card-badge-categoria.doce { background: var(--accent-color); }

.card-favorite-btn {
  position: absolute;
  top: 0px;
  right: 0px;
  width: 36px;
  height: 36px;
  border: 2px solid #fff;
  background: white;
  color: var(--header-color);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1rem;
  transition: all 0.2s ease;
}

.card-favorite-btn:hover {
  background: var(--favorite-color);
  color: white;
  border: none;
}

.card-favorite-btn.active:hover {
  color: white;
}

.card-favorite-btn.active {
  color: #e74c3c;
}

.card-favorite-btn.animando {
  animation: pulse 0.3s ease;
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.3); }
}

.card-content {
  padding: 16px 18px;
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.card-titulo {
  font-family: 'Playfair Display', serif;
  font-size: 1.15rem;
  color: var(--header-color);
      line-height: 1.3;
  font-weight: 700;
  margin: 0;
}

.card-meta {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding-bottom: 12px;
  border-bottom: 1px dashed rgba(139, 0, 0, 0.15);
}

.card-meta-row {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.card-meta-item {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  font-size: 0.8rem;
  color: var(--light-text);
  font-weight: 600;
}

.card-meta-item i {
  color: var(--accent-color);
  font-size: 0.85rem;
}

.card-actions {
  margin-top: auto;
}

.card-link {
  width: 100%;
  padding: 10px 14px;
  background: #fff;
  color: var(--header-color);
  border: 2px solid var(--header-color);
  font-family: inherit;
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  transition: all 0.2s ease;
}

.card-link:hover {
  background: var(--accent-color);
  color: #fff;
  border: 2px solid var(--accent-color);
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.2);
}

.card-link i {
  font-size: 0.85rem;
}

@media (max-width: 768px) {
  .card-image { height: 140px; }
  .card-content { padding: 12px; }
  .card-titulo { font-size: 0.95rem; }
  .card-meta-item { font-size: 0.7rem; }
}

@media (max-width: 480px) {
  .card-image { height: 110px; }
  .card-content { padding: 10px; }
  .card-titulo { font-size: 0.85rem; }
  .card-link { font-size: 0.65rem; padding: 8px; }
  .card-favorite-btn { width: 30px; height: 30px; font-size: 0.85rem; }
}



================================================================
ARQUIVO: components\Home\Filtros\Filtros.css
================================================================
.recipe-filters {
  background: #fff;
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.15);
  padding: 24px 28px;
  margin: 30px auto;
}

.recipe-filters h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.3rem;
  color: var(--header-color);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin-bottom: 18px;
  display: flex;
  align-items: center;
  gap: 10px;
}

.recipe-filters h3 i {
  color: var(--accent-color);
}

.filtros-grid {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr;
  gap: 12px;
}

.filter-group {
  position: relative;
}

.filter-icon {
  position: absolute;
  left: 14px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--accent-color);
  font-size: 0.9rem;
  pointer-events: none;
}

.filter-group input,
.filter-group select {
  width: 100%;
  padding: 12px 15px 12px 40px;
  border: 2px solid rgba(139, 0, 0, 0.2);
  background: #faf8f5;
  font-family: inherit;
  font-size: 0.9rem;
  color: #333;
  outline: none;
  transition: all 0.25s ease;
  box-sizing: border-box;
  height: 46px;
}

.filter-group input:focus,
.filter-group select:focus {
  border-color: var(--header-color);
  background: #fff;
  box-shadow: 0 0 0 3px rgba(139, 0, 0, 0.1);
}

.filter-buttons {
  display: flex;
  gap: 10px;
  grid-column: 1 / -1;
  justify-content: flex-end;
  margin-top: 8px;
}

.filter-buttons button {
  padding: 11px 22px;
  border: 2px solid var(--header-color);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.78rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  transition: all 0.25s ease;
}

.filter-buttons button:first-child {
  background: var(--header-color);
  color: #fff;
}

.filter-buttons button:first-child:hover {
  background: #a00000;
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.25);
}

.filter-buttons button:last-child {
  color: var(--light-text);
  border: none;
}

@media (max-width: 768px) {
  .recipe-filters {
    padding: 18px;
  }

  .filtros-grid {
    grid-template-columns: 1fr;
  }

  .filter-buttons {
    flex-direction: column;
  }

  .filter-buttons button {
    width: 100%;
    justify-content: center;
  }
}



================================================================
ARQUIVO: components\Home\Input\Input.css
================================================================
.input-with-icon {
  position: relative;
  margin-top: 8px;
}

.input-with-icon .form-control-edit {
  width: 100%;
  padding: 12px 15px 12px 42px;
  border: 2px solid rgba(139, 0, 0, 0.2);
  font-size: 0.95rem;
  background: #faf8f5;
  box-sizing: border-box;
  transition: all 0.25s ease;
  outline: none;
  height: 48px;
  font-family: inherit;
}

.input-with-icon .form-control-edit:focus {
  border-color: var(--header-color);
  background: #fff;
  box-shadow: 0 0 0 3px rgba(139, 0, 0, 0.1);
}

.input-with-icon .form-control-edit::placeholder {
  color: transparent;
}

.input-with-icon label {
  position: absolute;
  left: 42px;
  top: 50%;
  transform: translateY(-50%);
  color: #999;
  font-weight: 500;
  font-size: 0.9rem;
  pointer-events: none;
  transition: all 0.3s ease;
  background: transparent;
  padding: 0 4px;
}

.input-with-icon .form-control-edit:focus ~ label,
.input-with-icon .form-control-edit:not(:placeholder-shown) ~ label {
  top: -10px;
  font-size: 0.7rem;
  color: var(--header-color);
  background: #fff;
  padding: 0 6px;
  transform: translateY(0);
}

.input-with-icon i {
  position: absolute;
  left: 14px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--accent-color);
  z-index: 1;
  transition: all 0.3s ease;
  font-size: 0.9rem;
}

.input-with-icon .form-control-edit:focus ~ i,
.input-with-icon .form-control-edit:not(:placeholder-shown) ~ i {
  color: var(--header-color);
}

.input-toggle-senha {
  position: absolute;
  right: 14px;
  top: 50%;
  transform: translateY(-50%);
  cursor: pointer;
  color: #999;
  z-index: 2;
  font-size: 1rem;
  transition: color 0.25s ease;
}

.input-toggle-senha:hover {
  color: var(--header-color);
}



================================================================
ARQUIVO: components\Modal\Modal.css
================================================================
.modal-overlay {
  position: fixed;
  inset: 0;
  z-index: 2000;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px 15px;
  background: rgba(0, 0, 0, 0.5);
  backdrop-filter: blur(5px);
  overflow-y: auto;
  animation: fadeInModal 0.2s ease;
}

.modal-content {
  background: #fff;
  border: 2px solid var(--header-color);
  border-radius: 4px;
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.15);
  position: relative;
  width: 100%;
  max-width: 600px;
  max-height: 90vh;
  display: flex;
  flex-direction: column;
  animation: slideInModal 0.3s ease;
  overflow: hidden;
}

.modal-header {
  background: var(--header-color);
  color: #fff;
  padding: 18px 24px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-bottom: 2px solid var(--header-color);
  flex-shrink: 0;
}

.modal-header h2 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.2rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin: 0;
  display: flex;
  align-items: center;
  gap: 10px;
}

.modal-close {
  font-size: 1.8rem;
  line-height: 1;
  color: #fff;
  cursor: pointer;
  opacity: 0.7;
  transition: all 0.3s ease;
  -webkit-user-select: none;
  user-select: none;
}

.modal-close:hover {
  opacity: 1;
  transform: rotate(90deg);
}

.modal-body {
  padding: 24px 28px;
  overflow-y: auto;
  flex: 1;
}

.modal-body.form-body {
  padding: 22px 26px;
}

.modal-footer {
  border-top: 2px dashed rgba(139, 0, 0, 0.2);
  padding: 14px 24px;
  display: flex;
  gap: 12px;
  justify-content: flex-end;
  background: #faf8f5;
  flex-shrink: 0;
}

.modal-actions {
  display: flex;
  gap: 12px;
  justify-content: flex-end;
  margin-top: 16px;
  padding-top: 18px;
  border-top: 2px dashed rgba(139, 0, 0, 0.2);
}

.modal-btn {
  padding: 11px 22px;
  border: 2px solid;
  font-family: inherit;
  font-weight: 700;
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  min-width: 130px;
  transition: all 0.25s ease;
}

.modal-btn-primary {
  background: var(--accent-color);
  border-color: var(--accent-color);
  color: white;
}

.modal-btn-primary:hover:not(:disabled) {
  background: var(--header-color);
  border-color: var(--header-color);
  color: #fff;
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.2);
}

.modal-btn-save {
  background: #27ae60;
  border-color: #27ae60;
  color: #fff;
}

.modal-btn-save:hover:not(:disabled) {
  background: #1e8449;
  border-color: #1e8449;
  box-shadow: 4px 4px 0 rgba(39, 174, 96, 0.3);
}

.modal-btn-cancel {
  border: none;
  color: var(--light-text);
}


.modal-btn-danger {
  background: #e74c3c;
  border-color: #e74c3c;
  color: #fff;
}

.modal-btn-danger:hover:not(:disabled) {
  background: #c0392b;
  border-color: #c0392b;
  box-shadow: 4px 4px 0 rgba(231, 76, 60, 0.3);
}

.modal-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.modal-message {
  padding: 12px 15px;
  margin-bottom: 16px;
  font-weight: 600;
  font-size: 0.88rem;
  display: flex;
  align-items: center;
  gap: 10px;
  border-left: 4px solid;
}

.modal-message-success {
  background: rgba(39, 174, 96, 0.1);
  border-left-color: #27ae60;
  color: #1e8449;
}

.modal-message-error {
  background: rgba(231, 76, 60, 0.1);
  border-left-color: #e74c3c;
  color: #e74c3c;
}

.modal-message-info {
  background: rgba(52, 152, 219, 0.1);
  border-left-color: #3498db;
  color: #2874a6;
}

@keyframes fadeInModal {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes slideInModal {
  from { opacity: 0; transform: translateY(-30px) scale(0.95); }
  to { opacity: 1; transform: translateY(0) scale(1); }
}

@media (max-width: 600px) {
  .modal-content {
    max-width: 100%;
    max-height: 95vh;
  }

  .modal-header {
    padding: 16px 20px;
  }

  .modal-header h2 {
    font-size: 1rem;
  }

  .modal-body {
    padding: 20px;
  }

  .modal-footer {
    flex-direction: column-reverse;
    padding: 14px 20px;
  }

  .modal-actions {
    flex-direction: column-reverse;
  }

  .modal-btn {
    width: 100%;
  }
}

.modal-body .form-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  margin-bottom: 20px;
}

.modal-body .form-group {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.modal-body .form-group label {
  font-size: 0.78rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--header-color);
}

.modal-body .form-group input,
.modal-body .form-group select,
.modal-body .form-group textarea {
  padding: 12px 14px;
  border: 2px solid rgba(139, 0, 0, 0.2);
  background: #faf8f5;
  font-family: inherit;
  font-size: 0.92rem;
  color: #333;
  outline: none;
  transition: all 0.25s ease;
  width: 100%;
  box-sizing: border-box;
}

.modal-body .form-group textarea {
  resize: vertical;
}

.modal-body .form-group input:focus,
.modal-body .form-group select:focus,
.modal-body .form-group textarea:focus {
  border-color: var(--header-color);
  background: #fff;
  box-shadow: 0 0 0 3px rgba(139, 0, 0, 0.1);
}

.modal-body .modal-warning {
  background: rgba(231, 76, 60, 0.08);
  border-left: 4px solid #e74c3c;
  color: #e74c3c;
  padding: 10px 14px;
  font-size: 0.85rem;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 12px;
}

@media (max-width: 600px) {
  .modal-body .form-grid {
    grid-template-columns: 1fr;
  }
}

.modal-body::-webkit-scrollbar,
.modal-content::-webkit-scrollbar,
.modal-body *::-webkit-scrollbar,
.modal-content *::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}

.modal-body::-webkit-scrollbar-track,
.modal-content::-webkit-scrollbar-track,
.modal-body *::-webkit-scrollbar-track,
.modal-content *::-webkit-scrollbar-track {
  background: transparent;
}

.modal-body::-webkit-scrollbar-thumb,
.modal-content::-webkit-scrollbar-thumb,
.modal-body *::-webkit-scrollbar-thumb,
.modal-content *::-webkit-scrollbar-thumb {
  background: rgba(139, 0, 0, 0.2);
  border-radius: 999px;
  transition: background 0.25s ease;
}

.modal-body::-webkit-scrollbar-thumb:hover,
.modal-content::-webkit-scrollbar-thumb:hover,
.modal-body *::-webkit-scrollbar-thumb:hover,
.modal-content *::-webkit-scrollbar-thumb:hover {
  background: rgba(139, 0, 0, 0.45);
}

.modal-body,
.modal-content,
.modal-body *,
.modal-content * {
  scrollbar-width: thin;
  scrollbar-color: rgba(139, 0, 0, 0.2) transparent;
}



================================================================
ARQUIVO: components\Modal\ModalCadastro\ModalCadastro.css
================================================================
.cadastro-form-group {
  position: relative;
  margin-bottom: 16px;
}

.cadastro-form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  margin-bottom: 16px;
}

.cadastro-form-row .cadastro-form-group {
  margin-bottom: 0;
}

.cadastro-password-toggle {
  position: absolute;
  right: 15px;
  top: 50%;
  transform: translateY(-50%);
  cursor: pointer;
  color: #999;
  z-index: 2;
  transition: all 0.3s ease;
}

.cadastro-password-toggle:hover {
  color: var(--header-color);
}

.cadastro-login-link {
  text-align: center;
  margin-top: 18px;
  font-size: 0.85rem;
  color: var(--light-text);
}

.cadastro-login-link a {
  color: var(--header-color);
  font-weight: 700;
}

.cadastro-login-link a:hover {
  text-decoration: underline;
}

@media (max-width: 600px) {
  .cadastro-form-row {
    grid-template-columns: 1fr;
  }
}



================================================================
ARQUIVO: components\Modal\ModalLogin\ModalLogin.css
================================================================
.login-form-group {
  position: relative;
  margin-bottom: 18px;
}

.login-options {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 18px;
}

.remember-me {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 0.85rem;
  color: var(--light-text);
  cursor: pointer;
  -webkit-user-select: none;
  user-select: none;
}

.remember-me input {
  cursor: pointer;
  accent-color: var(--header-color);
}

.login-register-link {
  text-align: center;
  margin-top: 18px;
  font-size: 0.85rem;
  color: var(--light-text);
}

.login-register-link a {
  color: var(--header-color);
  font-weight: 700;
}

.login-register-link a:hover {
  text-decoration: underline;
}



================================================================
ARQUIVO: components\Modal\ModalLogout\ModalLogout.css
================================================================
.logout-modal-box {
display: none;
position: fixed;
z-index: 2000;
left: 0;
top: 0;
width: 100%;
height: 100%;
background-color: rgba(0, 0, 0, 0.5);
backdrop-filter: blur(5px);
overflow-y: auto;
}

.logout-modal-box.show {
display: flex;
align-items: center;
justify-content: center;
padding: 20px 15px;
}

.logout-modal-content {
background: #fff;
border: 2px solid var(--header-color);
border-radius: 4px;
box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.15);
padding: 35px 40px 30px;
max-width: 420px;
width: 100%;
text-align: center;
position: relative;
animation: modalFadeIn 0.3s ease;
margin: auto;
}

.logout-modal-close {
position: absolute;
top: 15px;
right: 18px;
background: none;
border: none;
font-size: 1.8rem;
color: #ccc;
cursor: pointer;
transition: all 0.3s ease;
padding: 0 5px;
opacity: 0.7;
line-height: 1;
}

.logout-modal-icon {
width: 70px;
height: 70px;
background: var(--header-color);
border: 2px solid var(--header-color);
display: flex;
align-items: center;
justify-content: center;
margin: 0 auto 22px;
box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.15);
}

.logout-modal-icon i {
font-size: 1.5rem;
color: white;
}

.logout-modal-title {
font-family: 'DM Serif Display', serif;
font-size: 1.3rem;
font-weight: 700;
color: var(--header-color);
margin-bottom: 10px;
text-transform: uppercase;
letter-spacing: 0.5px;
}

.logout-modal-message {
color: var(--light-text);
font-size: 0.95rem;
margin-bottom: 26px;
line-height: 1.5;
}

.logout-modal-actions {
display: flex;
gap: 12px;
justify-content: center;
}

.logout-btn-cancel,
.logout-btn-confirm {
padding: 11px 22px;
font-family: inherit;
font-weight: 700;
font-size: 0.8rem;
cursor: pointer;
transition: all 0.25s ease;
display: inline-flex;
align-items: center;
justify-content: center;
gap: 8px;
flex: 1;
text-transform: uppercase;
letter-spacing: 0.5px;
border: 2px solid var(--header-color);
}

.logout-btn-cancel {
color: var(--light-text);
border: none;
}

.logout-btn-confirm {
background: var(--header-color);
color: #fff;
border: none;
}

@media (min-width: 769px) {
.logout-modal-close:hover {
opacity: 1;
transform: rotate(90deg);
}


.logout-btn-confirm:hover {
    box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.25);
}


}

@media (max-width: 768px) {
.logout-modal-box.show {
padding: 20px 5px;
}
}

@media (max-width: 480px) {
.logout-modal-content {
padding: 28px 20px 22px;
}


.logout-modal-icon {
    width: 60px;
    height: 60px;
}

.logout-modal-icon i {
    font-size: 1.7rem;
}

.logout-modal-title {
    font-size: 1.1rem;
}

.logout-modal-message {
    font-size: 0.88rem;
}

.logout-modal-actions {
    flex-direction: column;
    gap: 10px;
}

.logout-btn-cancel,
.logout-btn-confirm {
    flex: none;
    width: 100%;
    font-size: 0.75rem;
    padding: 10px 18px;
}


}

@keyframes modalFadeIn {
from {
opacity: 0;
transform: translateY(-50px) scale(0.95);
}


to {
    opacity: 1;
    transform: translateY(0) scale(1);
}

}



================================================================
ARQUIVO: components\Modal\ModalNotificacoes\ModalNotificacoes.css
================================================================
.notificacoes-body {
  padding: 12px;
}

.notificacoes-vazia {
  text-align: center;
  padding: 50px 20px;
}

.notificacoes-vazia i {
  font-size: 3rem;
  color: var(--header-color);
  opacity: 0.3;
  display: block;
  margin-bottom: 12px;
}

.notificacoes-vazia h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.1rem;
  color: var(--header-color);
  margin-bottom: 6px;
}

.notificacoes-vazia p {
  color: var(--light-text);
  font-size: 0.88rem;
}

.notificacao-item {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  padding: 14px 16px;
  border: 2px solid rgba(139, 0, 0, 0.1);
  background: #faf8f5;
  margin-bottom: 10px;
  cursor: pointer;
  transition: all 0.25s ease;
  position: relative;
}

.notificacao-item:hover {
  border-color: var(--header-color);
  transform: translateX(4px);
}

.notificacao-item.nao-lida {
  background: #fff;
  border-left: 4px solid var(--accent-color);
}

.notificacao-item.nao-lida:hover {
  border-left-color: var(--header-color);
}

.notificacao-icon {
  flex-shrink: 0;
  width: 40px;
  height: 40px;
  background: var(--header-color);
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.1rem;
}

.notificacao-item.nao-lida .notificacao-icon {
  background: var(--accent-color);
  color: white;
}

.notificacao-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.notificacao-mensagem {
  font-size: 0.88rem;
  color: #333;
  font-weight: 500;
  line-height: 1.4;
  margin: 0;
}

.notificacao-item.nao-lida .notificacao-mensagem {
  font-weight: 700;
}

.notificacao-data {
  font-size: 0.72rem;
  color: var(--light-text);
  font-weight: 600;
}

.notificacao-nao-lida-indicador {
  width: 10px;
  height: 10px;
  background: var(--accent-color);
  border-radius: 50%;
  flex-shrink: 0;
  margin-top: 4px;
}

.notificacoes-footer {
  justify-content: space-between;
}

.notificacoes-marcar-todas,
.notificacoes-excluir-todas {
  flex: 1;
  padding: 10px 14px;
  background: #fff;
  border: 2px solid var(--header-color);
  color: var(--header-color);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.7rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  transition: all 0.25s ease;
}

.notificacoes-marcar-todas:hover:not(:disabled) {
  background: var(--header-color);
  color: #fff;
}

.notificacoes-excluir-todas {
  color: #e74c3c;
  border-color: #e74c3c;
}

.notificacoes-excluir-todas:hover:not(:disabled) {
  background: #e74c3c;
  color: #fff;
}

.notificacoes-marcar-todas:disabled,
.notificacoes-excluir-todas:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

@media (max-width: 600px) {
  .notificacoes-footer {
    flex-direction: column;
  }

  .notificacao-item {
    padding: 12px;
    gap: 10px;
  }

  .notificacao-icon {
    width: 34px;
    height: 34px;
    font-size: 1rem;
  }
}

.notificacoes-marcar-todas i,
.notificacoes-excluir-todas i {
  color: inherit;
  font-size: 0.9rem;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}



================================================================
ARQUIVO: components\Modal\ModalPerfil\ModalPerfil.css
================================================================
.user-view-info {
  display: flex;
  flex-direction: column;
  gap: 14px;
  margin-bottom: 10px;
}

.user-view-item {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 12px 16px;
  background: #faf8f5;
  border: 2px solid rgba(139, 0, 0, 0.1);
  border-left: 4px solid var(--accent-color);
}

.user-view-label {
  font-size: 0.72rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.6px;
  color: var(--header-color);
}

.user-view-value {
  font-size: 0.95rem;
  color: #333;
  font-weight: 600;
  word-break: break-word;
}

.user-form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  margin-bottom: 16px;
}

.user-form-group {
  display: flex;
  flex-direction: column;
}

.user-input-float {
  position: relative;
}

.user-input-float .user-form-control-float {
  width: 100%;
  padding: 12px 42px 12px 42px;
  border: 2px solid rgba(139, 0, 0, 0.2);
  border-radius: 4px;
  font-size: 0.92rem;
  background: #faf8f5;
  box-sizing: border-box;
  transition: all 0.25s ease;
  outline: none;
  height: 48px;
  font-family: inherit;
  color: #333;
}

.user-input-float .user-form-control-float:focus {
  border-color: var(--header-color);
  background: #fff;
  box-shadow: 0 0 0 3px rgba(139, 0, 0, 0.1);
}

.user-input-float .user-form-control-float::placeholder {
  color: transparent;
}

.user-input-float .user-form-control-float[readonly] {
  background-color: #f0ece6;
  color: #888;
  cursor: not-allowed;
}

.user-input-float label {
  position: absolute;
  left: 42px;
  top: 50%;
  transform: translateY(-50%);
  color: #999;
  font-weight: 500;
  font-size: 0.88rem;
  pointer-events: none;
  transition: all 0.3s ease;
  background: transparent;
  padding: 0 4px;
}

.user-input-float .user-form-control-float:focus ~ label,
.user-input-float .user-form-control-float:not(:placeholder-shown) ~ label {
  top: -10px;
  font-size: 0.7rem;
  color: var(--header-color);
  background: #fff;
  padding: 0 6px;
  transform: translateY(0);
}

.user-input-float i {
  position: absolute;
  left: 14px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--accent-color);
  z-index: 1;
  transition: all 0.3s ease;
  font-size: 0.9rem;
}

.user-input-float .user-form-control-float:focus ~ i,
.user-input-float .user-form-control-float:not(:placeholder-shown) ~ i {
  color: var(--header-color);
}

.user-password-toggle {
  position: absolute;
  right: 14px;
  top: 50%;
  transform: translateY(-50%);
  cursor: pointer;
  color: #999;
  z-index: 2;
  transition: all 0.3s ease;
  font-size: 1rem;
}

.user-password-toggle:hover {
  color: var(--header-color);
}

@media (max-width: 600px) {
  .user-form-row {
    grid-template-columns: 1fr;
    gap: 14px;
  }
}



================================================================
ARQUIVO: components\Modal\ModalResumoFavoritos\ModalResumoFavoritos.css
================================================================
.resumo-favoritos {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.resumo-card {
  display: flex;
  align-items: center;
  gap: 16px;
  padding: 16px 18px;
  border: 2px solid var(--header-color);
  background: #fff;
}

.resumo-card-icon {
  width: 48px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.4rem;
  flex-shrink: 0;
  color: #fff;
}

.resumo-card-total .resumo-card-icon {
  background: #e91e63;
}

.resumo-card-info {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.resumo-card-label {
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.6px;
  color: var(--light-text);
}

.resumo-card-value {
  font-family: 'DM Serif Display', serif;
  font-size: 1.8rem;
  font-weight: 800;
  color: var(--header-color);
  line-height: 1;
}

.resumo-ingrediente {
  padding: 16px 18px;
  background: #fff;
  border: 2px solid var(--header-color);
}

.resumo-ingrediente h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1rem;
  color: var(--header-color);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin: 0 0 12px 0;
  display: flex;
  align-items: center;
  gap: 8px;
}

.resumo-ingrediente h3 i {
  color: var(--accent-color);
}

.resumo-ingrediente-select-wrapper {
  position: relative;
}

.resumo-ingrediente-icon {
  position: absolute;
  left: 14px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--accent-color);
  font-size: 0.9rem;
  pointer-events: none;
}

.resumo-ingrediente-select {
  width: 100%;
  padding: 12px 15px 12px 40px;
  border: 2px solid rgba(139, 0, 0, 0.2);
  background: #faf8f5;
  font-family: inherit;
  font-size: 0.9rem;
  color: #333;
  outline: none;
  transition: all 0.25s ease;
  box-sizing: border-box;
  height: 46px;
  cursor: pointer;
}

.resumo-ingrediente-select:focus {
  border-color: var(--header-color);
  background: #fff;
  box-shadow: 0 0 0 3px rgba(139, 0, 0, 0.1);
}

.resumo-ingrediente-loading {
  margin-top: 12px;
  font-size: 0.85rem;
  color: var(--light-text);
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 8px;
}

.resumo-ingrediente-loading i {
  color: var(--accent-color);
}

.resumo-ingrediente-erro {
  margin-top: 12px;
  font-size: 0.85rem;
  color: #e74c3c;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 8px;
}

.resumo-ingrediente-resultado {
  margin-top: 14px;
  padding-top: 14px;
  border-top: 2px dashed rgba(139, 0, 0, 0.2);
}

.resumo-ingrediente-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  margin-bottom: 12px;
  flex-wrap: wrap;
}

.resumo-ingrediente-nome {
  font-family: 'DM Serif Display', serif;
  font-size: 1rem;
  color: var(--header-color);
  font-weight: 700;
}

.resumo-ingrediente-count {
  font-size: 0.72rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  background: var(--accent-color);
  color: white;
  padding: 4px 12px;
}

.resumo-ingrediente-lista {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.resumo-ingrediente-lista li {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 9px 12px;
  background: #faf8f5;
  border: 2px solid rgba(139, 0, 0, 0.1);
  border-left: 4px solid var(--accent-color);
  font-size: 0.85rem;
  font-weight: 600;
  color: #333;
}

.resumo-ingrediente-lista li i {
  color: var(--header-color);
  font-size: 0.85rem;
  flex-shrink: 0;
}

.resumo-repetidos {
  margin-top: 4px;
  padding: 16px 18px;
  background: #faf8f5;
  border: 2px dashed rgba(139, 0, 0, 0.25);
}

.resumo-repetidos h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1rem;
  color: var(--header-color);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin: 0 0 12px 0;
  display: flex;
  align-items: center;
  gap: 8px;
}

.resumo-repetidos h3 i {
  color: var(--accent-color);
}

.resumo-lista {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.resumo-lista li {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 6px 12px;
  background: #fff;
  border: 2px solid var(--header-color);
  font-size: 0.82rem;
  font-weight: 600;
  color: var(--header-color);
}

.resumo-lista li i {
  color: #27ae60;
  font-size: 0.75rem;
}

.resumo-vazio {
  font-size: 0.88rem;
  color: var(--light-text);
  font-style: italic;
  margin: 0;
}

@media (max-width: 600px) {
  .resumo-card-value {
    font-size: 1.5rem;
  }

  .resumo-card-icon {
    width: 42px;
    height: 42px;
    font-size: 1.2rem;
  }

  .resumo-ingrediente-header {
    flex-direction: column;
    align-items: flex-start;
  }
}



================================================================
ARQUIVO: components\Modal\ModalVerReceita\ModalVerReceita.css
================================================================
.ver-receita-grid {
  display: grid;
  grid-template-columns: 240px 1fr 1fr 1fr;
  gap: 20px;
  align-items: start;
}

.ver-receita-col {
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.ver-receita-col-imagem {
  justify-content: flex-start;
}

.ver-receita-imagem {
  width: 100%;
  overflow: hidden;
  border: 2px solid var(--header-color);
  background: #f5f0eb;
}

.ver-receita-meta {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.ver-receita-meta-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 9px 12px;
  background: #faf8f5;
  border: 2px solid rgba(139, 0, 0, 0.1);
  border-left: 4px solid var(--accent-color);
  font-size: 0.82rem;
  color: #333;
}

.ver-receita-meta-item i {
  color: var(--header-color);
  font-size: 0.9rem;
  flex-shrink: 0;
}

.ver-receita-meta-item strong {
  color: var(--header-color);
}

.ver-receita-section h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 0.95rem;
  color: var(--header-color);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin-bottom: 12px;
  padding-bottom: 10px;
  border-bottom: 1px dashed rgba(139, 0, 0, 0.2);
  display: flex;
  align-items: center;
  gap: 8px;
}

.ver-receita-section h3 i {
  color: var(--accent-color);
}

.ver-receita-list {
  padding-left: 18px;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.ver-receita-list li {
  font-size: 0.85rem;
  color: var(--light-text);
  line-height: 1.5;
}

@media (max-width: 900px) {
  .ver-receita-grid {
    grid-template-columns: 1fr 1fr;
  }

  .ver-receita-col-imagem {
    grid-column: 1 / -1;
  }

  .ver-receita-imagem {
    height: 240px;
  }
}

@media (max-width: 600px) {
  .ver-receita-grid {
    grid-template-columns: 1fr;
  }
}



================================================================
ARQUIVO: index.css
================================================================
@import './styles/variables.css';

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  min-height: 100vh;
  min-height: 100dvh;
  background-color: var(--main-bg-color);
  background-image: url("data:image/svg+xml,%3Csvg width='100' height='100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M0 0h100v100H0z' fill='%23f8f5f0'/%3E%3Cpath d='M0 0l100 100M100 0L0 100' stroke='%23e8e1d3' stroke-width='0.9'/%3E%3C/svg%3E");
  background-repeat: repeat;
  background-attachment: fixed;
}

body {
  min-height: 100vh;
  min-height: 100dvh;
  display: flex;
  flex-direction: column;
  width: 100%;
  max-width: 100% !important;
  margin: 0 !important;
  padding: 0 !important;
  font-family: 'Inter', sans-serif;
  color: var(--text-color);
  line-height: 1.7;
  background: transparent !important;
  position: relative;
  overflow-x: hidden;
}

header,
main,
footer {
  background-color: transparent;
}

main {
  flex: 1 0 auto;
  width: 100%;
}

footer {
  flex-shrink: 0;
}

.page-wrapper {
  /* min-height: 100vh; */
  display: flex;
  flex-direction: column;
  flex: 1;
}

#root {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes modalFadeIn {
  from { opacity: 0; transform: translateY(-50px) scale(0.95); }
  to { opacity: 1; transform: translateY(0) scale(1); }
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.p-toast {
  z-index: 5000 !important;
  pointer-events: none !important;
}

.p-toast .p-toast-message {
  border-radius: 0;
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.2);
  font-family: inherit;
   pointer-events: auto !important;
}

.p-toast .p-toast-message-content {
  padding: 14px 16px;
  gap: 12px;
}

.p-toast .p-toast-summary {
  font-family: 'DM Serif Display', serif;
  font-size: 1rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.p-toast .p-toast-detail {
  font-size: 0.85rem;
  font-weight: 600;
  margin-top: 4px;
}

.p-toast .p-toast-message-success {
  background: #fff;
  border-left: 6px solid #27ae60;
}

.p-toast .p-toast-message-success .p-toast-message-icon,
.p-toast .p-toast-message-success .p-toast-summary {
  color: #27ae60;
}

.p-toast .p-toast-message-error {
  background: #fff;
  border-left: 6px solid #e74c3c;
}

.p-toast .p-toast-message-error .p-toast-message-icon,
.p-toast .p-toast-message-error .p-toast-summary {
  color: #e74c3c;
}

.p-toast .p-toast-message-info {
  background: #fff;
  border-left: 6px solid var(--accent-color);
}

.p-toast .p-toast-message-info .p-toast-message-icon,
.p-toast .p-toast-message-info .p-toast-summary {
  color: var(--accent-color)
}

.p-toast .p-toast-message-info .p-toast-detail {
  color: var(--accent-color);
}

.p-toast .p-toast-message-warn {
  background: #fff;
  border-left: 6px solid #f39c12;
}

.p-toast .p-toast-message-warn .p-toast-message-icon,
.p-toast .p-toast-message-warn .p-toast-summary {
  color: #f39c12;
}

@media (max-width: 768px) {
  .p-toast {
    width: 100%;
    left: 0;
    right: 0;
    padding: 0 12px;
  }

  .p-toast-top-right,
  .p-toast-top-left,
  .p-toast-bottom-right,
  .p-toast-bottom-left {
    width: 100%;
    max-width: 100%;
    left: 0;
    right: 0;
    padding: 12px;
  }

  .p-toast .p-toast-message {
    margin: 0 0 10px 0;
    width: 100%;
  }

  .p-toast .p-toast-message-content {
    padding: 12px 14px;
    gap: 10px;
  }

  .p-toast .p-toast-summary {
    font-size: 0.9rem;
  }

  .p-toast .p-toast-detail {
    font-size: 0.8rem;
  }

  .p-toast .p-toast-icon-close {
    width: 20px;
    height: 20px;
  }
}

@media (max-width: 480px) {
  .p-toast .p-toast-message-content {
    padding: 10px 12px;
  }

  .p-toast .p-toast-summary {
    font-size: 0.85rem;
  }

  .p-toast .p-toast-detail {
    font-size: 0.75rem;
  }
}




================================================================
ARQUIVO: pages\Admin\Carrossel-Admin\Carrossel.css
================================================================
.carrossel-btn-novo {
  padding: 12px 20px;
  background: var(--header-color);
  color: #fff;
  border: 2px solid var(--header-color);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.85rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  transition: all 0.25s ease;
  box-shadow: 3px 3px 0 rgba(139, 0, 0, 0.2);
}

.carrossel-btn-novo:hover {
  background: #a00000;
  box-shadow: 5px 5px 0 rgba(139, 0, 0, 0.3);
}

.carrossel-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 22px;
  padding: 25px 35px;
}

.carrossel-card {
  background: #fff;
  border: 2px solid var(--header-color);
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.15);
  display: flex;
  flex-direction: column;
  transition: all 0.25s ease;
}

.carrossel-card:hover {
  box-shadow: 7px 7px 0 rgba(139, 0, 0, 0.22);
}

.carrossel-card-img {
  position: relative;
  width: 100%;
  height: 180px;
  overflow: hidden;
  background: #faf8f5;
  border-bottom: 2px solid var(--header-color);
}

.carrossel-card-ordem {
  position: absolute;
  top: 10px;
  left: 10px;
  background: var(--header-color);
  color: #fff;
  padding: 4px 10px;
  font-size: 0.75rem;
  font-weight: 800;
  letter-spacing: 0.5px;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  border: 2px solid #fff;
  box-shadow: 2px 2px 0 rgba(0,0,0,0.2);
}

.carrossel-card-status {
  position: absolute;
  top: 10px;
  right: 10px;
  padding: 4px 10px;
  font-size: 0.72rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.6px;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  border: 2px solid #fff;
  box-shadow: 2px 2px 0 rgba(0,0,0,0.2);
}

.carrossel-card-status.ativo {
  background: #27ae60;
  color: #fff;
}

.carrossel-card-status.inativo {
  background: #e74c3c;
  color: #fff;
}

.carrossel-card-body {
  padding: 16px 18px;
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.carrossel-card-body h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.15rem;
  color: var(--header-color);
  font-weight: 700;
  margin: 0;
}

.carrossel-card-body p {
  font-size: 0.88rem;
  color: var(--light-text);
  line-height: 1.5;
  margin: 0;
}

.carrossel-card-link {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-size: 0.78rem;
  color: var(--accent-color);
  font-weight: 700;
  background: rgba(212, 175, 55, 0.12);
  padding: 4px 10px;
  border: 1px dashed var(--accent-color);
  align-self: flex-start;
  word-break: break-all;
  margin-top: 4px;
}

.carrossel-card-actions {
  display: flex;
  border-top: 2px solid var(--header-color);
}

.carrossel-action-btn {
  flex: 1;
  padding: 11px 8px;
  background: #fff;
  border: none;
  border-right: 1px solid rgba(139, 0, 0, 0.15);
  color: var(--header-color);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  transition: all 0.2s ease;
}

.carrossel-action-btn:last-child {
  border-right: none;
}

.carrossel-action-btn.edit:hover {
  background: var(--accent-color);
  color: white;
}

.carrossel-action-btn.toggle:hover {
  background: var(--header-color);
  color: #fff;
}

.carrossel-action-btn.delete {
  color: #e74c3c;
}

.carrossel-action-btn.delete:hover {
  background: #e74c3c;
  color: #fff;
}

.carrossel-empty {
  text-align: center;
  padding: 80px 20px;
  background: #fff;
  border: 2px dashed rgba(139, 0, 0, 0.3);
  margin: 25px 35px;
}

.carrossel-empty i {
  font-size: 3.5rem;
  color: var(--header-color);
  opacity: 0.3;
  margin-bottom: 15px;
  display: block;
}

.carrossel-empty h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.4rem;
  color: var(--header-color);
  margin-bottom: 8px;
}

.carrossel-empty p {
  color: var(--light-text);
}

@media (max-width: 768px) {
  .carrossel-grid {
    grid-template-columns: 1fr;
    padding: 0px;
    gap: 5px;
  }

  .carrossel-empty {
    margin: 15px;
  }
}



================================================================
ARQUIVO: pages\Admin\Dashboard-Admin\Dashboard.css
================================================================
.dashboard-cards {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 18px;
  padding: 25px 35px;
}

.dashboard-card {
  background: #fff;
  border: 2px solid var(--header-color);
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.15);
  display: flex;
  align-items: center;
  gap: 16px;
  padding: 20px;
  transition: all 0.25s ease;
  min-width: 0;
}

.dashboard-card:hover {
  box-shadow: 7px 7px 0 rgba(139, 0, 0, 0.2);
}

.card-icon {
  width: 50px;
  height: 50px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.35rem;
  color: #fff;
  flex-shrink: 0;
}

.card-icon-users { background: var(--header-color); }
.card-icon-active { background: #2980b9; }
.card-icon-pending { background: var(--accent-color); color: var(--header-color); }
.card-icon-approved { background: #27ae60; }
.card-icon-rejected { background: #e74c3c; }
.card-icon-fav { background: #c0392b; }

.card-info {
  display: flex;
  flex-direction: column;
  gap: 4px;
  min-width: 0;
}

.card-label {
  font-size: 0.72rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.6px;
  color: var(--light-text);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.card-value {
  font-family: 'DM Serif Display', serif;
  font-size: 1.8rem;
  font-weight: 700;
  color: var(--header-color);
  line-height: 1;
}

.dashboard-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 25px;
  padding: 0 35px 35px;
}

.dashboard-panel {
  background: #fff;
  border: 2px solid var(--header-color);
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.15);
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.panel-header {
  background: var(--header-color);
  color: #fff;
  padding: 16px 22px;
  border-bottom: 2px solid var(--header-color);
}

.panel-header h2 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.05rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  display: flex;
  align-items: center;
  gap: 10px;
  margin: 0;
}

.panel-header h2 i {
  font-size: 1.1rem;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  line-height: 1;
}

.panel-body-chart {
  flex: 1;
  height: 340px;
  padding: 22px;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.panel-body-chart canvas {
  max-height: 100%;
}

.top-list {
  display: flex;
  flex-direction: column;
  gap: 14px;
  padding: 22px;
}

.top-item {
  display: grid;
  grid-template-columns: 42px 1fr auto;
  align-items: center;
  gap: 16px;
}

.top-rank {
  width: 42px;
  height: 42px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'DM Serif Display', serif;
  font-size: 1.2rem;
  font-weight: 800;
  background: var(--header-color);
  color: #fff;
  border: 2px solid var(--header-color);
  flex-shrink: 0;
}

.top-rank.rank-1 {
  background: var(--accent-color);
  color: var(--header-color);
  border-color: var(--accent-color);
  box-shadow: 3px 3px 0 rgba(139, 0, 0, 0.2);
}

.top-rank.rank-2 {
  background: #b0b0b0;
  border-color: #b0b0b0;
}

.top-rank.rank-3 {
  background: #cd7f32;
  border-color: #cd7f32;
}

.top-info {
  display: flex;
  flex-direction: column;
  gap: 6px;
  min-width: 0;
}

.top-titulo {
  font-size: 0.9rem;
  font-weight: 700;
  color: var(--header-color);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.top-bar-bg {
  width: 100%;
  height: 10px;
  background: rgba(139, 0, 0, 0.08);
  border: 1px solid rgba(139, 0, 0, 0.15);
  overflow: hidden;
}

.top-bar-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--header-color), #b30000);
  transition: width 0.6s ease;
}

.top-item:nth-child(1) .top-bar-fill {
  background: linear-gradient(90deg, var(--accent-color), #e6c84a);
}

.top-count {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-family: 'DM Serif Display', serif;
  font-size: 1.15rem;
  font-weight: 800;
  color: var(--header-color);
  min-width: 60px;
  justify-content: flex-end;
}

.top-count i {
  color: #e74c3c;
  font-size: 0.85rem;
}

.top-receitas-empty {
  text-align: center;
  padding: 40px 20px;
  color: var(--light-text);
}

.top-receitas-empty i {
  font-size: 2.5rem;
  opacity: 0.3;
  margin-bottom: 10px;
  display: block;
  color: var(--header-color);
}

.top-receitas-empty p {
  margin: 0;
  font-size: 0.9rem;
  font-weight: 600;
}

@media (max-width: 1100px) {
  .dashboard-cards {
    grid-template-columns: repeat(2, 1fr);
    padding: 20px 25px;
  }

  .dashboard-grid {
    padding: 0 25px 25px;
  }
}

@media (max-width: 900px) {
  .dashboard-grid {
    grid-template-columns: 1fr;
    gap: 18px;
  }

  .panel-body-chart {
    height: 320px;
  }
}

@media (max-width: 768px) {
  .dashboard-cards {
    padding: 15px;
    gap: 12px;
  }

  .dashboard-grid {
    padding: 0 15px 20px;
  }

  .dashboard-card {
    padding: 16px;
    gap: 12px;
  }

  .card-icon {
    width: 42px;
    height: 42px;
    font-size: 1.15rem;
  }

  .card-value {
    font-size: 1.5rem;
  }

  .panel-header {
    padding: 14px 18px;
  }

  .panel-header h2 {
    font-size: 0.95rem;
  }

  .panel-body-chart {
    padding: 15px;
    height: 280px;
  }
}

@media (max-width: 600px) {
  .top-item {
    grid-template-columns: 34px 1fr auto;
    gap: 12px;
  }

  .top-rank {
    width: 34px;
    height: 34px;
    font-size: 1rem;
  }

  .top-titulo {
    font-size: 0.82rem;
  }

  .top-count {
    font-size: 1rem;
    min-width: 46px;
  }
}

@media (max-width: 480px) {
  .dashboard-cards {
    grid-template-columns: 1fr;
  }

  .panel-body-chart {
    height: 240px;
  }
}



================================================================
ARQUIVO: pages\Admin\Receitas-Admin\ReceitasAprovadas.css
================================================================
.admin-thumb {
  width: 50px;
  height: 50px;
  object-fit: cover;
  border: 2px solid var(--header-color);
  display: block;
}

.receita-categoria {
  padding: 4px 10px;
  font-size: 0.7rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  display: inline-block;
  border: 2px solid;
}

.receita-categoria.salgado {
  background: rgba(139, 0, 0, 0.1);
  color: var(--header-color);
  border-color: var(--header-color);
}

.receita-categoria.doce {
  background: rgba(212, 175, 55, 0.15);
  color: #8b6914;
  border-color: var(--accent-color);
}

.admin-modal-large {
  max-width: 800px;
}

.detalhes-imagem {
  margin-bottom: 20px;
  border: 2px solid var(--header-color);
}

.detalhes-imagem img {
  width: 100%;
  max-height: 280px;
  object-fit: cover;
  display: block;
}

.detalhes-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  margin-bottom: 22px;
  padding-bottom: 16px;
  border-bottom: 1px dashed rgba(139, 0, 0, 0.2);
}

.detalhes-meta span {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 5px 12px;
  background: rgba(212, 175, 55, 0.12);
  border: 1px dashed var(--accent-color);
  font-size: 0.78rem;
  font-weight: 600;
  color: var(--header-color);
}

.detalhes-meta i {
  color: var(--accent-color);
}

.admin-modal-body h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1rem;
  color: var(--header-color);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin: 20px 0 12px;
  display: flex;
  align-items: center;
  gap: 8px;
  padding-bottom: 8px;
  border-bottom: 2px solid var(--header-color);
}

.admin-modal-body h3 i {
  color: var(--accent-color);
}

.admin-modal-body ul,
.admin-modal-body ol {
  padding-left: 22px;
  display: flex;
  flex-direction: column;
  gap: 6px;
  font-size: 0.88rem;
  color: #333;
}

.admin-modal-body textarea {
  padding: 12px 14px;
  border: 2px solid rgba(139, 0, 0, 0.2);
  background: #faf8f5;
  font-family: inherit;
  font-size: 0.92rem;
  color: #333;
  outline: none;
  transition: all 0.25s ease;
  resize: vertical;
  width: 100%;
  box-sizing: border-box;
}

.admin-modal-body textarea:focus {
  border-color: var(--header-color);
  background: #fff;
  box-shadow: 0 0 0 3px rgba(139, 0, 0, 0.1);
}

.admin-modal-body .form-grid {
  margin-bottom: 16px;
}

.admin-modal-body .form-group {
  margin-bottom: 14px;
}

.action-btn.btn-view {
  color: var(--accent-color);
}

.btn-save:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
  box-shadow: none;
}

@media (max-width: 768px) {
  .admin-thumb {
    width: 40px;
    height: 40px;
  }

  .admin-modal-large {
    max-width: 100%;
  }
}

.admin-thumb-wrapper {
  width: 50px;
  height: 50px;
  border: 2px solid var(--header-color);
  background: #faf8f5;
  overflow: hidden;
  display: inline-block;
  vertical-align: middle;
}

.admin-thumb-wrapper .admin-thumb {
  border: none;
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

@media (max-width: 768px) {
  .admin-thumb-wrapper {
    width: 40px;
    height: 40px;
  }
}

.admin-thumb-wrapper.imagem-loader-fallback,
.imagem-loader-fallback.admin-thumb-wrapper {
  width: 50px;
  height: 50px;
  min-height: 0;
  padding: 0;
  gap: 0;
  border: 2px solid var(--header-color);
  font-size: 0;
}

.imagem-loader-fallback.admin-thumb-wrapper i {
  font-size: 1.2rem;
}

.imagem-loader-fallback.admin-thumb-wrapper span {
  display: none;
}

@media (max-width: 768px) {
  .admin-thumb-wrapper.imagem-loader-fallback,
  .imagem-loader-fallback.admin-thumb-wrapper {
    width: 40px;
    height: 40px;
  }

  .imagem-loader-fallback.admin-thumb-wrapper i {
    font-size: 1rem;
  }
}



================================================================
ARQUIVO: pages\Admin\Receitas-Admin\ReceitasPendentes.css
================================================================
.admin-recipe-tabs {
  display: flex;
  gap: 8px;
}

.admin-recipe-tab {
display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 12px 20px;
    color: var(--light-text);
    background: transparent;
    border: 2px solid transparent;
    border-bottom: none;
    font-family: inherit;
    font-weight: 700;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    text-decoration: none;
    transition: all 0.25s ease;
    cursor: pointer;
    position: relative;
    margin-bottom: -2px;
}

.admin-recipe-tab:hover {
  background: rgba(139, 0, 0, 0.04);
}

.admin-recipe-tab.active {
    color: var(--header-color);
    border-color: rgba(139, 0, 0, 0.15);
    border-bottom: none;
}

.admin-recipe-tab span {
display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 22px;
    height: 22px;
    padding: 0 6px;
    background: rgba(139, 0, 0, 0.1);
    color: var(--header-color);
    font-size: 0.7rem;
    border-radius: 50%;
}

.admin-cards-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 22px;
  padding: 25px 35px;
}

.admin-recipe-card {
  background: #fff;
  border: 2px solid var(--header-color);
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.15);
  display: flex;
  flex-direction: column;
  transition: all 0.25s ease;
}

.admin-recipe-card:hover {
  box-shadow: 7px 7px 0 rgba(139, 0, 0, 0.2);
}

.admin-recipe-image {
  position: relative;
  width: 100%;
  height: 180px;
  overflow: hidden;
  background: #f0ece6;
  border-bottom: 2px solid var(--header-color);
}

.admin-recipe-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.admin-recipe-badge {
  position: absolute;
  top: 10px;
  right: 10px;
  padding: 5px 12px;
  font-size: 0.7rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  display: inline-flex;
  align-items: center;
  gap: 5px;
  color: #fff;
  border: 2px solid #fff;
  background: var(--accent-color);
  color: var(--header-color);
}

.admin-recipe-badge.rejected {
  background: #e74c3c;
  color: #fff;
}

.admin-recipe-content {
  padding: 18px 20px;
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.admin-recipe-content h2 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.15rem;
  color: var(--header-color);
  margin: 0;
}

.admin-recipe-meta {
  display: flex;
  gap: 14px;
  flex-wrap: wrap;
  font-size: 0.8rem;
  color: var(--light-text);
  font-weight: 600;
  padding-bottom: 12px;
  border-bottom: 1px dashed rgba(139, 0, 0, 0.2);
}

.admin-recipe-meta span {
  display: inline-flex;
  align-items: center;
  gap: 5px;
}

.admin-recipe-meta i {
  color: var(--accent-color);
}

.admin-recipe-actions {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  margin-top: auto;
}

.admin-btn {
  padding: 9px 14px;
  border: 2px solid;
  font-family: inherit;
  font-weight: 700;
  font-size: 0.72rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  transition: all 0.25s ease;
  flex: 1;
  justify-content: center;
}

.admin-btn-view {
  background: #fff;
  border-color: var(--header-color);
  color: var(--header-color);
}

.admin-btn-view:hover {
  background: var(--header-color);
  color: #fff;
}

.admin-btn-approve {
  background: #27ae60;
  border-color: #27ae60;
  color: #fff;
}

.admin-btn-approve:hover {
  background: #1e8449;
}

.admin-btn-reject {
  background: #e74c3c;
  border-color: #e74c3c;
  color: #fff;
}

.admin-btn-reject:hover {
  background: #b91c1c;
}

.admin-rejection-reason {
  background: rgba(231, 76, 60, 0.08);
  border-left: 4px solid #e74c3c;
  padding: 10px 14px;
  font-size: 0.82rem;
  color: #c0392b;
  display: flex;
  gap: 8px;
  align-items: flex-start;
  width: 100%;
}

.detalhes-imagem {
  margin-bottom: 20px;
  border: 2px solid var(--header-color);
}

.detalhes-imagem img {
  width: 100%;
  max-height: 280px;
  object-fit: cover;
  display: block;
}

.detalhes-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  margin-bottom: 22px;
  padding-bottom: 16px;
  border-bottom: 1px dashed rgba(139, 0, 0, 0.2);
}

.detalhes-meta span {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 5px 12px;
  background: rgba(212, 175, 55, 0.12);
  border: 1px dashed var(--accent-color);
  font-size: 0.78rem;
  font-weight: 600;
  color: var(--header-color);
}

.detalhes-meta i {
  color: var(--accent-color);
}

.admin-modal-body h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1rem;
  color: var(--header-color);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin: 20px 0 12px;
  display: flex;
  align-items: center;
  gap: 8px;
  padding-bottom: 8px;
  border-bottom: 2px solid var(--header-color);
}

.admin-modal-body h3 i {
  color: var(--accent-color);
}

.admin-modal-body ul,
.admin-modal-body ol {
  padding-left: 22px;
  display: flex;
  flex-direction: column;
  gap: 6px;
  font-size: 0.88rem;
  color: #333;
}

.admin-table-header{
      display: flex;
    align-items: center;
    gap: 8px;
    width: 100%;
    margin-bottom: 20px;
    padding: 18px 25px 0;
    border-bottom: 2px solid rgba(139, 0, 0, 0.15);
}

.admin-modal-body textarea {
  padding: 12px 14px;
  border: 2px solid rgba(139, 0, 0, 0.2);
  background: #faf8f5;
  font-family: inherit;
  font-size: 0.92rem;
  color: #333;
  outline: none;
  transition: all 0.25s ease;
  resize: vertical;
  width: 100%;
  box-sizing: border-box;
}

.admin-modal-body textarea:focus {
  border-color: var(--header-color);
  background: #fff;
  box-shadow: 0 0 0 3px rgba(139, 0, 0, 0.1);
}

.btn-delete:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
  box-shadow: none;
}

@media (max-width: 768px) {
  .admin-cards-grid {
    grid-template-columns: 1fr;
    padding: 15px;
  }

  .admin-recipe-tabs {
    flex-direction: column;
    width: 100%;
  }

  .admin-recipe-tab {
    justify-content: space-between;
  }
}



================================================================
ARQUIVO: pages\Admin\Usuarios-Admin\Usuarios.css
================================================================
.admin-table .status-badge {
  position: static !important;
  top: auto !important;
  right: auto !important;
  left: auto !important;
  bottom: auto !important;
  display: inline-block !important;
  padding: 4px 12px !important;
  font-size: 0.7rem !important;
  font-weight: 800 !important;
  text-transform: uppercase !important;
  letter-spacing: 1px !important;
  border-radius: 0 !important;
}

.admin-table .status-badge.active {
  background: rgba(46, 204, 113, 0.1) !important;
  color: #27ae60 !important;
  border: 2px solid #27ae60 !important;
}

.admin-table .status-badge.inactive {
  background: rgba(231, 76, 60, 0.1) !important;
  color: #e74c3c !important;
  border: 2px solid #e74c3c !important;
}

.action-btn.btn-edit {
  color: var(--header-color);
}

.action-btn.btn-delete {
  color: #e74c3c;
}

.switch {
  position: relative;
  display: inline-block;
  width: 44px;
  height: 22px;
}

.switch input {
  opacity: 0;
  width: 0;
  height: 0;
}

.slider {
  position: absolute;
  cursor: pointer;
  inset: 0;
  background-color: #e0d8d0;
  border: 2px solid var(--header-color);
  transition: 0.25s;
}

.slider:before {
  position: absolute;
  content: "";
  height: 12px;
  width: 12px;
  left: 3px;
  bottom: 3px;
  background-color: var(--header-color);
  transition: 0.25s;
}

input:checked + .slider {
  background-color: var(--header-color);
}

input:checked + .slider:before {
  transform: translateX(20px);
  background-color: #fff;
}

.admin-modal-delete-text {
  font-size: 1rem;
  margin-bottom: 12px;
  text-align: center;
}

.admin-modal-delete-text strong {
  color: var(--header-color);
}



================================================================
ARQUIVO: pages\Client\DetalhesReceita-Cliente\DetalhesReceita.css
================================================================
.detalhe-container {
  padding: 80px 30px 60px;
  max-width: 1250px;
  margin: 0 auto;
}

.detalhe-card {
  background: #fff;
  border: 2px solid var(--header-color);
  box-shadow: 6px 6px 0 rgba(139, 0, 0, 0.15);
}

.detalhe-hero {
  display: grid;
  grid-template-columns: 1fr 1fr;
  min-height: 400px;
  border-bottom: 2px solid var(--header-color);
}

.detalhe-image {
  position: relative;
  overflow: hidden;
  border-right: 2px solid var(--header-color);
  background: #f0ece6;
}

.detalhe-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.detalhe-info {
  padding: 40px 35px;
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.detalhe-badge-categoria {
  display: inline-block;
  align-self: flex-start;
  padding: 6px 14px;
  font-size: 0.75rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.8px;
  color: #fff;
}

.detalhe-badge-categoria.salgado { background: var(--header-color); }
.detalhe-badge-categoria.doce { background: var(--accent-color); color: var(--header-color); }

.detalhe-info-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 20px;
  flex-wrap: wrap;
}

.detalhe-titulo {
  font-family: 'DM Serif Display', serif;
  font-size: 2.2rem;
  color: var(--header-color);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin: 0;
  line-height: 1.1;
}

.detalhe-info-actions {
  display: flex;
  gap: 10px;
}

.detalhe-favorite-btn {
  padding: 10px 16px;
  border: none;
  color: var(--light-text);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.72rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  transition: all 0.25s ease;
}

.detalhe-print-btn {
  padding: 10px 16px;
  border: 2px solid var(--header-color);
  background: #fff;
  color: var(--header-color);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.72rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  transition: all 0.25s ease;
}

.detalhe-favorite-btn:hover,
.detalhe-print-btn:hover {
  background: var(--header-color);
  color: #fff;
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.2);
}

.detalhe-favorite-btn.active {
  background: #e74c3c;
  color: #fff;
  border-color: #e74c3c;
}

.detalhe-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  padding-top: 20px;
  border-top: 1px dashed rgba(139, 0, 0, 0.2);
}

.detalhe-meta-item {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 6px 14px;
  background: rgba(212, 175, 55, 0.12);
  border: 1px dashed var(--accent-color);
  font-size: 0.82rem;
  font-weight: 600;
  color: var(--header-color);
}

.detalhe-meta-item i {
  color: var(--accent-color);
}

.detalhe-body {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 40px;
  padding: 40px 35px;
}

.detalhe-section-header {
  display: flex;
  align-items: center;
  gap: 12px;
  padding-bottom: 14px;
  border-bottom: 2px solid var(--header-color);
  margin-bottom: 22px;
}

.detalhe-section-header i {
  color: var(--accent-color);
  font-size: 1.2rem;
}

.detalhe-section-header h2 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.15rem;
  color: var(--header-color);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin: 0;
}

.detalhe-section-count {
  margin-left: auto;
  font-size: 0.8rem;
  color: var(--light-text);
  background: rgba(212, 175, 55, 0.06);
  padding: 4px 14px;
  border-radius: 20px;
  font-weight: 600;
  white-space: nowrap;
}

.detalhe-ingredientes {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.detalhe-ingrediente-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 16px;
  background: #faf8f5;
  border: 2px solid rgba(139, 0, 0, 0.1);
  transition: all 0.2s ease;
}

.detalhe-ingrediente-item:hover {
  border-color: var(--header-color);
  transform: translateX(4px);
}

.detalhe-ingrediente-item i {
  color: var(--accent-color);
  font-size: 0.6rem;
}

.detalhe-ingrediente-text {
  font-size: 0.92rem;
  color: #333;
  font-weight: 500;
}

.detalhe-preparo {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.detalhe-preparo-item {
  display: flex;
  gap: 16px;
  padding: 14px 18px;
  background: #faf8f5;
  border: 2px solid rgba(139, 0, 0, 0.1);
  transition: all 0.2s ease;
}

.detalhe-preparo-item:hover {
  border-color: var(--header-color);
  transform: translateX(4px);
}

.detalhe-preparo-number {
  flex-shrink: 0;
  min-width: 32px;
  height: 32px;
  background: var(--header-color);
  color: #fff;
  font-weight: 800;
  font-size: 0.85rem;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.detalhe-preparo-text {
  font-size: 0.92rem;
  color: #333;
  line-height: 1.6;
  font-weight: 500;
}

.detalhe-ingrediente-label {
  display: flex;
  align-items: center;
  gap: 12px;
  width: 100%;
  cursor: pointer;
  -webkit-user-select: none;
  user-select: none;
}

.detalhe-ingrediente-label input[type="checkbox"] {
  position: absolute;
  opacity: 0;
  width: 0;
  height: 0;
  pointer-events: none;
}

.detalhe-ingrediente-check {
  width: 20px;
  height: 20px;
  flex-shrink: 0;
  border: 2px solid var(--header-color);
  background: #fff;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  transition: all 0.2s ease;
  position: relative;
}

.detalhe-ingrediente-label input[type="checkbox"]:checked + .detalhe-ingrediente-check {
  background: #27ae60;
  border-color: #27ae60;
}

.detalhe-ingrediente-label input[type="checkbox"]:checked + .detalhe-ingrediente-check::after {
  content: "";
  width: 5px;
  height: 10px;
  border: solid #fff;
  border-width: 0 2px 2px 0;
  transform: rotate(45deg) translate(-1px, -2px);
}

.detalhe-ingrediente-item.marcado .detalhe-ingrediente-text {
  text-decoration: line-through;
  color: var(--light-text);
  opacity: 0.6;
}

.detalhe-ingrediente-item.marcado {
  background: #f0ece6;
}



================================================================
ARQUIVO: pages\Client\Home-Cliente\Home.css
================================================================
.home-wrapper {
  padding: 35px 5px 60px;
  max-width: 1300px;
  margin: 0 auto;
}

.tab-buttons {
  display: flex;
  gap: 10px;
  margin: 30px auto;
  max-width: 1200px;
  justify-content: flex-start;
  border-bottom: 2px solid rgba(139, 0, 0, 0.15);
}

.tab-button {
  padding: 12px 22px;
  background: transparent;
  border: 2px solid transparent;
  color: var(--header-color);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.85rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  transition: all 0.25s ease;
}

.tab-button:hover {
  color: var(--header-color);
  background: rgba(139, 0, 0, 0.04);
}

.tab-button.active {
  color: var(--header-color);
  border-color: rgba(139, 0, 0, 0.15);
  border-bottom: none;
}

.tab-button .badge {
  background: var(--accent-color);
  color: var(--header-color);
  padding: 2px 8px;
  border-radius: 50px;
  font-size: 0.7rem;
  font-weight: 800;
}

.category-section {
  margin: 40px auto;
}

.category-title {
  font-family: 'DM Serif Display', serif;
  font-size: 1.8rem;
  color: var(--header-color);
  text-transform: uppercase;
  letter-spacing: 1px;
  display: flex;
  align-items: center;
  gap: 12px;
  padding-bottom: 12px;
  border-bottom: 2px solid var(--header-color);
  margin-bottom: 24px;
}

.recipe-count {
  margin-left: auto;
  font-size: 0.8rem;
  font-family: 'Inter', sans-serif;
  color: var(--light-text);
  text-transform: none;
  letter-spacing: 0;
  font-weight: 600;
}

.receitas-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 18px;
}

.empty-category {
  text-align: center;
  padding: 60px 20px;
  background: #fff;
  border: 2px dashed rgba(139, 0, 0, 0.3);
}

.empty-category i {
  font-size: 3rem;
  color: var(--header-color);
  opacity: 0.3;
  margin-bottom: 12px;
  display: block;
}

.empty-category h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.2rem;
  color: var(--header-color);
  margin-bottom: 6px;
}

.empty-category p {
  color: var(--light-text);
  font-size: 0.9rem;
}

.favoritas-header {
  display: flex;
  justify-content: flex-end;
  margin-bottom: 20px;
}

.favoritas-resumo-btn {
  padding: 12px 20px;
  background: var(--header-color);
  color: #fff;
  border: 2px solid var(--header-color);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.78rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  transition: all 0.25s ease;
}

.favoritas-resumo-btn:hover:not(:disabled) {
  background: var(--accent-color);
  color: white;
  border-color: var(--accent-color);
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.25);
}

.favoritas-resumo-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

@media (max-width: 480px) {
  .favoritas-header {
    justify-content: stretch;
  }

  .favoritas-resumo-btn {
    width: 100%;
    justify-content: center;
    font-size: 0.72rem;
    padding: 10px 16px;
  }
}



================================================================
ARQUIVO: pages\Client\MinhasReceitas-Cliente\MinhasReceitas.css
================================================================
.minhas-wrapper {
  padding: 90px 30px 60px;
  width: 1300px;
  margin: 0 auto;
}

.minhas-stats {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 14px;
  margin-bottom: 30px;
}

.stat-card {
  background: #fff;
  border: 2px solid var(--header-color);
  box-shadow: 3px 3px 0 rgba(139, 0, 0, 0.15);
  padding: 16px;
  display: flex;
  align-items: center;
  gap: 12px;
  transition: all 0.25s ease;
}

.stat-icon {
  width: 42px;
  height: 42px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.1rem;
  color: #fff;
}

.stat-icon.total { background: var(--header-color); }
.stat-icon.aprovadas { background: #27ae60; }
.stat-icon.pendentes { background: var(--accent-color); }
.stat-icon.rejeitadas { background: #e74c3c; }
.stat-icon.curtidas { background: #e91e63; }

.stat-info {
  display: flex;
  flex-direction: column;
  line-height: 1.1;
  min-width: 0;
}

.stat-value {
  font-family: 'DM Serif Display', serif;
  font-size: 1.5rem;
  font-weight: 800;
  color: var(--header-color);
}

.stat-label {
  font-size: 0.7rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--light-text);
}

.minhas-tabs {
  display: flex;
  gap: 0;
  margin: 30px 0;
  border-bottom: 2px solid var(--header-color);
}

.minhas-tab {
  padding: 14px 24px;
  background: transparent;
  border: none;
  border-bottom: 4px solid transparent;
  color: var(--light-text);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.85rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 10px;
  transition: all 0.25s ease;
  margin-bottom: -2px;
}

.minhas-tab:hover {
  color: var(--header-color);
}

.minhas-tab.active {
  color: var(--header-color);
  border-bottom-color: var(--header-color);
}

.minhas-tab .tab-count {
  background: rgba(139, 0, 0, 0.1);
  color: var(--header-color);
  padding: 2px 10px;
  border-radius: 50px;
  font-size: 0.72rem;
  font-weight: 800;
}

.minhas-tab.active .tab-count {
  background: var(--accent-color);
}

.minhas-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 24px;
}

.minha-card {
  background: #fff;
  border: 2px solid var(--header-color);
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.15);
  display: flex;
  flex-direction: column;
  transition: all 0.25s ease;
}

.minha-card:hover {
  box-shadow: 7px 7px 0 rgba(139, 0, 0, 0.2);
}

.minha-imagem {
  position: relative;
  width: 100%;
  height: 180px;
  overflow: hidden;
  background: #f0ece6;
  border-bottom: 2px solid var(--header-color);
}

.status-badge {
  position: absolute;
  top: 5px;
  left: 5px;
  padding: 5px 12px;
  font-size: 0.7rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  display: inline-flex;
  align-items: center;
  gap: 5px;
  color: #fff;
  border: 2px solid #fff;
  box-shadow: 2px 2px 0 rgba(0,0,0,0.2);
}

.status-badge.pendente { background: var(--accent-color); }
.status-badge.aprovada { background: #27ae60; }
.status-badge.rejeitada { background: #e74c3c; }

.minha-info {
  padding: 16px 18px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  flex: 1;
}

.minha-info h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.15rem;
  color: var(--header-color);
  margin: 0;
  line-height: 1.2;
}

.minha-meta {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  padding-bottom: 12px;
  border-bottom: 1px dashed rgba(139, 0, 0, 0.2);
}

.minha-meta span {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  font-size: 0.78rem;
  color: var(--light-text);
  font-weight: 600;
}

.minha-meta span i {
  color: var(--accent-color);
  font-size: 0.8rem;
}

.minha-actions {
  margin-top: auto;
  display: flex;
  gap: 8px;
  flex-direction: column;
}

.btn-ver,
.btn-motivo,
.btn-excluir {
  flex: 1;
  min-width: 80px;
  padding: 10px 14px;
  background: #fff;
  font-family: inherit;
  font-weight: 700;
  font-size: 0.72rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  border: 2px solid;
  transition: all 0.25s ease;
}

.btn-ver {
  border-color: var(--header-color);
  color: var(--header-color);
}

.btn-ver:hover {
  background: var(--header-color);
  color: #fff;
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.2);
}

.btn-motivo {
  border-color: var(--accent-color);
  color: var(--accent-color);
}

.btn-motivo:hover {
  background: var(--accent-color);
  color: white;
  box-shadow: 4px 4px 0 rgba(212, 175, 55, 0.4);
}

.btn-excluir {
  border-color: #e74c3c;
  color: #e74c3c;
}

.btn-excluir:hover {
  background: #e74c3c;
  color: #fff;
  box-shadow: 4px 4px 0 rgba(231, 76, 60, 0.3);
}

.motivo-texto {
  font-size: 0.95rem;
  color: var(--light-text);
  line-height: 1.6;
  background: rgba(231, 76, 60, 0.08);
  border-left: 4px solid #e74c3c;
  padding: 14px 16px;
  margin-bottom: 8px;
}

.minhas-empty {
  text-align: center;
  padding: 80px 20px;
  background: #fff;
  border: 2px dashed rgba(139, 0, 0, 0.3);
}

.minhas-empty i {
  font-size: 3rem;
  color: var(--header-color);
  opacity: 0.3;
  margin-bottom: 12px;
  display: block;
}

.minhas-empty h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.2rem;
  color: var(--header-color);
  margin-bottom: 6px;
}

.minhas-empty p {
  color: var(--light-text);
  font-size: 0.9rem;
}

@media (max-width: 1024px) {
  .minhas-stats {
    grid-template-columns: repeat(3, 1fr);
  }

  .minhas-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 18px;
  }
}

@media (max-width: 768px) {
  .minhas-wrapper {
    padding: 80px 8px 40px;
    margin: 0;
  }

  .minhas-stats {
    grid-template-columns: repeat(2, 1fr);
    gap: 10px;
    margin-bottom: 20px;
  }

  .stat-card {
    padding: 12px;
    gap: 10px;
  }

  .stat-icon {
    width: 36px;
    height: 36px;
    font-size: 0.95rem;
  }

  .stat-value {
    font-size: 1.3rem;
  }

  .stat-label {
    font-size: 0.65rem;
  }

  .minhas-tabs {
    margin: 20px 0;
    overflow-x: auto;
    overflow-y: hidden;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
  }

  .minhas-tabs::-webkit-scrollbar {
    display: none;
  }

  .minhas-tab {
    padding: 12px 16px;
    font-size: 0.75rem;
    flex-shrink: 0;
  }

  .minhas-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
  }

  .minha-imagem {
    height: 130px;
  }

  .minha-info {
    padding: 12px 14px;
    gap: 10px;
  }

  .minha-info h3 {
    font-size: 0.95rem;
  }

  .minha-meta {
    gap: 8px;
    padding-bottom: 10px;
  }

  .minha-meta span {
    font-size: 0.72rem;
  }

  .minha-actions {
    gap: 6px;
  }

  .btn-ver,
  .btn-motivo,
  .btn-excluir {
    padding: 8px 10px;
    font-size: 0.68rem;
    min-width: 0;
  }
}

@media (max-width: 480px) {


  .minhas-stats {
    grid-template-columns: 1fr 1fr;
    gap: 8px;
  }

  .stat-card {
    padding: 10px;
    gap: 8px;
  }

  .stat-icon {
    width: 32px;
    height: 32px;
    font-size: 0.85rem;
  }

  .stat-value {
    font-size: 1.1rem;
  }

  .stat-label {
    font-size: 0.6rem;
  }

  .minhas-tab {
    padding: 10px 12px;
    font-size: 0.68rem;
    gap: 5px;
  }

  .minhas-tab .tab-count {
    display: none;
  }

  .minhas-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 8px;
  }

  .minha-imagem {
    height: 110px;
  }

  .status-badge {
    padding: 3px 8px;
    font-size: 0.58rem;
    gap: 3px;
  }

  .minha-info {
    padding: 10px;
    gap: 8px;
  }

  .minha-info h3 {
    font-size: 0.82rem;
  }

  .minha-meta {
    gap: 6px;
    padding-bottom: 8px;
  }

  .minha-meta span {
    font-size: 0.65rem;
  }

  .btn-ver,
  .btn-motivo,
  .btn-excluir {
    padding: 7px 8px;
    font-size: 0.62rem;
    gap: 4px;
  }
}



================================================================
ARQUIVO: pages\Client\NovaReceita-Cliente\NovaReceita.css
================================================================
.nova-main-layout {
  padding: 90px 30px 60px;
  max-width: 1300px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 805px;
  gap: 30px;
  align-items: start;
}

.nova-form-container {
  min-width: 0;
}

.nova-recipe-form {
  background: #fff;
  border: 2px solid var(--header-color);
  box-shadow: 5px 5px 0 rgba(139, 0, 0, 0.15);
  padding: 30px;
  display: flex;
  flex-direction: column;
  gap: 22px;
}

.nova-form-row {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  grid-template-rows: auto auto;
  column-gap: 14px;
  row-gap: 8px;
  align-items: end;
}

.nova-titulo-width { grid-column: span 2; }
.nova-tempo-width { grid-column: span 1; }
.nova-porcoes-width { grid-column: span 1; }

.nova-form-group {
  display: flex;
  flex-direction: column;
  gap: 8px;
  min-width: 0;
}

.nova-form-group label {
  font-weight: 700;
  font-size: 0.82rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--header-color);
  display: flex;
  align-items: center;
  gap: 8px;
  min-height: 28px;
}

.nova-form-group input[type="text"],
.nova-form-group input[type="number"],
.nova-form-group select {
  padding: 12px 15px;
  border: 2px solid rgba(139, 0, 0, 0.2);
  background: #faf8f5;
  font-family: inherit;
  font-size: 0.92rem;
  color: #333;
  outline: none;
  transition: all 0.25s ease;
  width: 100%;
  box-sizing: border-box;
  height: 46px;
}

.nova-form-group input:focus,
.nova-form-group select:focus {
  border-color: var(--header-color);
  background: #fff;
  box-shadow: 0 0 0 3px rgba(139, 0, 0, 0.1);
}

.nova-tempo-label-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 8px;
  min-height: 28px;
}

.nova-tempo-toggle {
  padding: 4px 10px;
  background: var(--accent-color);
  color: var(--header-color);
  border: 2px solid var(--accent-color);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 4px;
  transition: all 0.2s ease;
  white-space: nowrap;
}

.nova-tempo-toggle:hover {
  background: var(--header-color);
  color: #fff;
  border-color: var(--header-color);
}

.nova-tempo-horas {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
}

.nova-ingredientes-container,
.nova-passos-container {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.nova-ingrediente-item {
  padding: 0;
}

.nova-ingrediente-row {
  display: grid;
  grid-template-columns: 90px 160px 1fr;
  gap: 8px;
  align-items: center;
}

.nova-quantidade,
.nova-unidade,
.nova-ingrediente-nome,
.nova-passo-input {
  padding: 11px 14px;
  border: 2px solid rgba(139, 0, 0, 0.2);
  background: #faf8f5;
  font-family: inherit;
  font-size: 0.9rem;
  color: #333;
  outline: none;
  transition: all 0.25s ease;
  width: 100%;
  box-sizing: border-box;
}

.nova-quantidade:focus,
.nova-unidade:focus,
.nova-ingrediente-nome:focus,
.nova-passo-input:focus {
  border-color: var(--header-color);
  background: #fff;
  box-shadow: 0 0 0 3px rgba(139, 0, 0, 0.1);
}

.nova-ingrediente-nome-wrapper,
.nova-passo-wrapper {
  position: relative;
  width: 100%;
}

.nova-ingrediente-nome,
.nova-passo-input {
  padding-right: 44px;
}

.nova-remove-btn {
  position: absolute;
  right: 6px;
  top: 50%;
  transform: translateY(-50%);
  width: 30px;
  height: 30px;
  background: #fff;
  border: 2px solid #e74c3c;
  color: #e74c3c;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  transition: all 0.2s ease;
  font-size: 0.85rem;
}

.nova-remove-btn:hover {
  background: #e74c3c;
  color: #fff;
}

.nova-add-btn {
  align-self: flex-start;
  padding: 10px 18px;
  background: #fff;
  border: 2px solid var(--accent-color);
  color: var(--accent-color);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  transition: all 0.25s ease;
}

.nova-add-btn:hover {
  background: var(--accent-color);
  color: white;
}

.nova-file-label {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 14px 18px;
  background: #faf8f5;
  border: 2px dashed rgba(139, 0, 0, 0.3);
  cursor: pointer;
  transition: all 0.25s ease;
}

.nova-file-label:hover {
  border-color: var(--header-color);
  background: #fff;
}

.nova-file-text {
  font-size: 0.88rem;
  color: #666;
  font-weight: 600;
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.nova-file-button {
  flex-shrink: 0;
  width: 40px;
  height: 40px;
  color: var(--header-color);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 1rem;
}

.nova-image-preview {
  margin-top: 10px;
  border: 2px solid var(--header-color);
  background: #faf8f5;
  padding: 8px;
}

.nova-image-preview img {
  width: 100%;
  max-height: 250px;
  object-fit: cover;
  display: block;
}

.nova-form-erro {
  background: rgba(231, 76, 60, 0.1);
  border-left: 4px solid #e74c3c;
  color: #e74c3c;
  padding: 12px 15px;
  font-weight: 600;
  font-size: 0.9rem;
  display: flex;
  align-items: center;
  gap: 10px;
}

.nova-submit-button {
  padding: 16px 24px;
  background: var(--header-color);
  color: #fff;
  border: 2px solid var(--header-color);
  font-family: inherit;
  font-weight: 700;
  font-size: 0.9rem;
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  transition: all 0.25s ease;
}

.nova-submit-button:hover:not(:disabled) {
  background: var(--accent-color);
  color: white;
  border-color: var(--accent-color);
  box-shadow: 5px 5px 0 rgba(212, 175, 55, 0.4);
}

.nova-submit-button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
}

.nova-info-card {
  background: #fff;
  border: 2px solid var(--header-color);
  box-shadow: 5px 5px 0 rgba(139, 0, 0, 0.15);
  position: sticky;
  top: 100px;
}

.nova-info-header {
  background: var(--header-color);
  color: #fff;
  padding: 18px 22px;
  border-bottom: 2px solid var(--header-color);
}

.nova-info-header h3 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.1rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin: 0;
  display: flex;
  align-items: center;
  gap: 10px;
}

.nova-info-body {
  padding: 22px;
  display: flex;
  flex-direction: column;
  gap: 20px;
  max-height: calc(100vh - 180px);
  overflow-y: auto;
}

.nova-info-section {
  padding-bottom: 18px;
  border-bottom: 1px dashed rgba(139, 0, 0, 0.2);
}

.nova-info-section:last-child {
  border-bottom: none;
  padding-bottom: 0;
}

.nova-info-section h4 {
  font-size: 0.82rem;
  color: var(--header-color);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  font-weight: 700;
  margin-bottom: 10px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.nova-info-section p {
  font-size: 0.85rem;
  color: var(--light-text);
  line-height: 1.6;
}

.nova-info-section ul {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.nova-info-section li {
  display: flex;
  align-items: flex-start;
  gap: 8px;
  font-size: 0.82rem;
  color: var(--light-text);
  line-height: 1.5;
}

.nova-info-section li i {
  margin-top: 3px;
  flex-shrink: 0;
  font-size: 0.75rem;
}

.nova-info-section li i.pi-check { color: #27ae60; }
.nova-info-section li i.pi-times { color: #e74c3c; }

.nova-info-tip {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.nova-info-tip p {
  font-size: 0.82rem;
  color: var(--light-text);
  line-height: 1.5;
  align-items: flex-start;
}

.nova-info-tip p i {
  color: #27ae60;
  margin-top: 3px;
  flex-shrink: 0;
}

@media (max-width: 1000px) {
  .nova-main-layout {
    grid-template-columns: 1fr;
  }

  .nova-info-card {
    position: static;
  }
}

@media (max-width: 768px) {
  .nova-main-layout { padding: 80px 15px 40px; gap: 20px; }
  .nova-recipe-form { padding: 20px; }
  .nova-form-row { grid-template-columns: 1fr; gap: 12px; }
  .nova-titulo-width, .nova-tempo-width, .nova-porcoes-width { grid-column: span 1; }
  .nova-ingrediente-row { grid-template-columns: 1fr 1fr; }
  .nova-ingrediente-nome-wrapper { grid-column: 1 / -1; }
}

@media (max-width: 480px) {
  .nova-main-layout { padding: 70px 8px 0; }
  .nova-recipe-form { padding: 15px; }
  .nova-ingrediente-row { grid-template-columns: 1fr; }
  .nova-quantidade, .nova-unidade { grid-column: span 1; }
}

.nova-form-group-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 10px;
  flex-wrap: wrap;
  margin-bottom: 4px;
}

.nova-form-group-header label {
  margin: 0;
}

.nova-form-group-header .nova-add-btn {
  align-self: auto;
}

.nova-metade-width { grid-column: span 2; }

.nova-tempo-erro {
  margin-top: 6px;
  padding: 8px 12px;
  background: rgba(231, 76, 60, 0.1);
  border-left: 3px solid #e74c3c;
  color: #e74c3c;
  font-size: 0.78rem;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 8px;
}

.nova-tempo-erro i {
  font-size: 0.85rem;
}



================================================================
ARQUIVO: pages\Client\Sobre-Cliente\Sobre.css
================================================================
.sobre-container {
  padding: 120px 30px 60px;
  max-width: 950px;
  margin: 0 auto;
}

.sobre-header {
  text-align: center;
  margin-bottom: 40px;
  padding-bottom: 24px;
  border-bottom: 2px solid var(--header-color);
}

.sobre-header h1 {
  font-family: 'DM Serif Display', serif;
  font-size: 2.2rem;
  color: var(--header-color);
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 8px;
  display: inline-flex;
  align-items: center;
  gap: 12px;
}

.sobre-header h1 i {
  color: var(--accent-color);
}

.sobre-subtitulo {
  color: var(--light-text);
  font-size: 1rem;
  font-style: italic;
}

.sobre-content {
  display: flex;
  flex-direction: column;
  gap: 30px;
}

.sobre-section {
  background: #fff;
  border: 2px solid var(--header-color);
  box-shadow: 4px 4px 0 rgba(139, 0, 0, 0.15);
  padding: 28px 32px;
}

.sobre-section h2 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.3rem;
  color: var(--header-color);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin-bottom: 14px;
  display: flex;
  align-items: center;
  gap: 10px;
  padding-bottom: 12px;
  border-bottom: 1px dashed rgba(139, 0, 0, 0.2);
}

.sobre-section h2 i {
  color: var(--accent-color);
}

.sobre-section p {
  color: var(--light-text);
  line-height: 1.7;
  font-size: 0.95rem;
}

.sobre-section ul {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.sobre-section ul li {
  display: flex;
  align-items: center;
  gap: 10px;
  color: var(--light-text);
  font-size: 0.95rem;
}

.sobre-section ul li i {
  color: #27ae60;
}

.journey-timeline {
  display: flex;
  flex-direction: column;
  gap: 0;
  position: relative;
  padding-left: 10px;
}

.timeline-item {
  display: flex;
  gap: 20px;
  position: relative;
  padding-bottom: 24px;
}

.timeline-item:last-child {
  padding-bottom: 0;
}

.timeline-item::before {
  content: "";
  position: absolute;
  left: 52px;
  top: 32px;
  bottom: -8px;
  width: 2px;
  background: rgba(139, 0, 0, 0.15);
}

.timeline-item:last-child::before {
  display: none;
}

.timeline-year {
  flex-shrink: 0;
  width: 105px;
  height: 42px;
  background: var(--header-color);
  color: white;
  font-family: 'DM Serif Display', serif;
  font-weight: 700;
  font-size: 1rem;
  letter-spacing: 1px;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 2px solid var(--header-color);
  position: relative;
  z-index: 1;
}

.timeline-content {
  flex: 1;
  padding-top: 4px;
}

.timeline-content h4 {
  font-family: 'DM Serif Display', serif;
  font-size: 1.05rem;
  color: var(--header-color);
  margin-bottom: 6px;
}

.timeline-content p {
  color: var(--light-text);
  line-height: 1.6;
  font-size: 0.92rem;
  margin: 0;
}

.faq-container {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.faq-item {
  border: 2px solid rgba(139, 0, 0, 0.15);
  background: #faf8f5;
  transition: all 0.25s ease;
}

.faq-item.active {
  border-color: var(--header-color);
  background: #fff;
}

.faq-question {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  padding: 14px 18px;
  cursor: pointer;
  font-weight: 600;
  color: var(--header-color);
  font-size: 0.95rem;
}

.faq-question i {
  color: var(--accent-color);
  transition: transform 0.3s ease;
  font-size: 0.85rem;
}

.faq-item.active .faq-question i {
  transform: rotate(180deg);
}

.faq-answer {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.35s ease, padding 0.35s ease;
  padding: 0 18px;
}

.faq-item.active .faq-answer {
  max-height: 400px;
  padding: 0 18px 16px;
}

.faq-answer p {
  color: var(--light-text);
  line-height: 1.7;
  font-size: 0.92rem;
  margin: 0;
  padding-top: 4px;
  border-top: 1px dashed rgba(139, 0, 0, 0.15);
}

.sobre-contato {
  margin-top: 10px;
  font-weight: 700;
  color: var(--header-color) !important;
  display: flex;
  align-items: center;
  gap: 10px;
}

.sobre-contato i {
  color: var(--accent-color);
}




================================================================
ARQUIVO: styles\variables.css
================================================================
:root {
  --main-bg-color: #f8f5f0;
  --spining-bg: #f8f5f0de;
  --header-color: #a52a2a;
  --accent-color: #d4af37;
  --secondary-accent: #c19b76;
  --text-color: #333;
  --light-text: #777;
  --border-color: #8b4513;
  --card-bg: #fefdf9;
  --shadow: 0 5px 10px rgba(0, 0, 0, 0.08);
  --transition: all 0.3s ease;
  --favorite-color: #e74c3c;
  --header-bg: #a52a2a;
  --rejected-color: #e74c3c;
  
  --categoria-doce-bg: #fef3c7;
  --categoria-doce-color: #92400e;
  --categoria-doce-border: #f59e0b;
  --categoria-salgado-bg: #fee2e2;
  --categoria-salgado-color: #991b1b;
  --categoria-salgado-border: #ef4444;
}


